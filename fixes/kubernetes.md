# Kubernetes Lab — находки

Методичка: `kubernetes/Kubernetes_Lab_Plan.html`. Битых якорей в оглавлении нет.

## 🟢 Что хорошо
- apiVersion'ы актуальные: `apps/v1`, `autoscaling/v2` (HPA), `traefik.io/v1alpha1` (новая группа CRD Traefik v3,
  не устаревшая `traefik.containo.us`), `kind.x-k8s.io/v1alpha4`.
- README честно пишет «нужны пройденные Docker Lab и Traefik Lab».

## 🟡 Жёсткая зависимость от кода Traefik-лабы
Шаг 1.4: «Каталог api/ с Dockerfile и server.js — тот же самый, что вы собрали в Traefik-лабе… Скопируйте его»,
образ `traefik-lab-api:v1`. То есть без **пройденной** (а не просто прочитанной) Traefik-лабы шаги не выполнить.
Стоит сказать это в самом начале методички (блок «перед стартом»): «понадобится папка `api/` из Traefik-лабы».
Хорошо бы дать запасной вариант (ссылку на готовый `api/` в репозитории traefik-lab), если Traefik-лабу
проходили давно или не до конца.

## 🟡 Версии не указаны
Ни в README, ни в карточке, ни в методичке нет версии Kubernetes / kind. Это важно: `kind` по умолчанию берёт
образ узла своей версии, и через год поведение (например, HPA/metrics-server) может отличаться.
Предложение: в шаге 1.1 зафиксировать образ узла (`kind create cluster --image kindest/node:v1.3x.x`) и
написать в стеке «проверено на kind 0.2x / Kubernetes 1.3x».

## 🟡 Мелкие несостыковки с Traefik-лабой
- Traefik-лаба: `postgres:16`, здесь — `postgres:16-alpine`. Не ошибка, но раз смысл лабы «переносим тот же стек»,
  лучше взять тот же тег (или пояснить, зачем alpine).
- Traefik закреплён на `v3.1` в обеих лабах — консистентно. Актуальная ветка v3 уже новее (3.5+); на v3.1 всё работает,
  но при обновлении обеих лаб — обновлять вместе.

## Метрики
Для HPA нужен metrics-server (упоминается 11 раз). В kind он обычно требует флаг `--kubelet-insecure-tls` —
проверь, что этот флаг есть в шаге установки (автоматической проверкой не определить, глазами — 1 минута).


---

## ✍️ Варианты решений

Оставь в каждом вопросе **один** вариант (остальные удали или поставь `[x]` у нужного).
Потом я прочитаю этот файл и перепишу лабу ровно по выбранному. ⭐ — моя рекомендация.

### 1. Лабе нужен код `api/` из Traefik-лабы
- [ ] ⭐ A — добавить блок «Перед стартом»: что понадобится папка `api/` из Traefik-лабы, и ссылку на готовую `api/` в репозитории traefik-lab на случай, если Traefik проходился давно
- [ ] B — положить копию `api/` прямо в kubernetes-lab, чтобы лаба была полностью самодостаточной
- [ ] C — оставить как есть

### 2. Версии Kubernetes / kind нигде не указаны
- [ ] ⭐ A — зафиксировать образ узла в шаге 1.1 (`kindest/node:v1.xx`) и написать в стеке «проверено на kind … / Kubernetes …»
- [ ] B — оставить без версий

### 3. `postgres:16-alpine` здесь и `postgres:16` в Traefik-лабе
- [ ] ⭐ A — взять ровно тот же тег, что в Traefik-лабе (смысл лабы — «тот же стек»)
- [ ] B — оставить alpine и дописать, зачем

### 4. Traefik `v3.1` (в Traefik-лабе тоже `v3.1`)
- [ ] ⭐ A — оставить `v3.1` в обеих лабах
- [ ] B — обновить обе лабы до последней 3.x

### 5. Флаг `--kubelet-insecure-tls` для metrics-server в kind
- [ ] ⭐ A — проверить шаг установки metrics-server и добавить флаг, если его нет
- [ ] B — не трогать

---

## 📖 Вычитка методички

### Часть 1 (оглавление, разделы 0–9, шаг 1.1)
- **[противоречие] Шаг 1.1, врезка «ПРЕДПОЛАГАЕТСЯ»** — «Установлены kind, kubectl и Docker Desktop (или Docker Engine) уже работают — это ровно то, что использовалось в Docker/Traefik-лабах» → неверно: kind и kubectl в Docker-лабе и Traefik-лабе не использовались вообще (это Compose/Podman лабы без единого объекта Kubernetes), «оттуда» — только Docker Desktop/Engine. При этом в разделе 0 «Что нужно перед стартом» kind и kubectl тоже не упомянуты как то, что нужно поставить заранее — в шаге 1.1 они возникают из ниоткуда → переформулировать фразу (kind/kubectl — новые для этой лабы инструменты) и добавить их в список «Что нужно перед стартом» в разделе 0.
- **[противоречие] Раздел 2.1 «От сервиса в docker-compose.yml к манифесту»** — «В Traefik-лабе сервис описывался так (раздел 4 Traefik Lab)» → раздел 4 Traefik-лабы называется «Технологический стек и структура проекта» и содержит только таблицу стека и дерево каталогов — фрагмента `api: build: ./api networks: [...]` там нет. Реальный фрагмент находится в Шаге 2.1 «Backend API — api/Dockerfile» (внутри раздела 9 Traefik-лабы) → заменить ссылку на «Шаг 2.1 Traefik Lab».
- **[противоречие] Раздел 3.2 (врезка ПОД КАПОТОМ) и раздел 6 (вступление)** — «у API в Traefik-лабе (раздел 2.4)» и «Traefik-лаба (раздел 2.4) уже показала главную боль» → в Traefik-лабе номера «раздел 2.4» не существует (раздел 2 — «Как Traefik устроен внутри», без подразделов 2.x). Про round-robin + healthcheck для API на самом деле рассказывает Шаг 2.4 «Масштабирование API + healthcheck» (внутри раздела 9 Traefik-лабы) → в обоих местах заменить на «Шаг 2.4 Traefik Lab».
- **[тех] Раздел 1.2, таблица vs врезка «ПОД КАПОТОМ»** — «...controller-manager... и если меньше трёх — просит scheduler создать недостающие» → противоречит собственной таблице чуть выше, где у scheduler написано «для каждого нового Pod решает, на какой Node его запустить» (то есть только размещение). Недостающие Pod-объекты создаёт сам контроллер (ReplicaSet-controller, часть controller-manager), а scheduler лишь назначает Node уже созданным, но ещё не запланированным подам → переформулировать: «controller-manager сам создаёт недостающие Pod'ы, а scheduler решает, на каком Node каждый из них запустить».
- **[противоречие] Оглавление (сайдбар) vs заголовки разделов** — сайдбар даёт «9. Пошаговая сборка (3 сессии)», «12. Вопросы для самопроверки», «13. Что дальше», а фактические заголовки этих же разделов в тексте — «9. Пошаговая сборка проекта (3 сессии)», «12. Вопросы для самопроверки / собеседования», «13. Что дальше: дорожная карта после лабы» → привести формулировки в сайдбаре и в заголовках разделов к одному варианту (во всех трёх случаях).
- **[противоречие] Раздел 4 «ConfigMap и Secret»** — пример `DB_NAME: "traefik_lab"` / `DB_PASSWORD: "supersecret"` не совпадает с реальными значениями Postgres из Traefik-лабы (`POSTGRES_DB: lab`, `POSTGRES_PASSWORD: labpass`); а главное — тот же `api/server.js`, который лаба в шаге 1.4 просит скопировать без изменений, вообще не читает `DB_HOST`/`DB_NAME`/`DB_PASSWORD` (в нём нет ни pg-клиента, ни обращения к `process.env` для БД) → либо явно оговорить, что ConfigMap/Secret здесь — чисто учебный пример «представим, что API подключается к БД», либо привести значения к реальным (`lab`/`labpass`) и показать, где в коде они бы применялись.
- — остальное в разделах 0–9 (шаг 1.1) проверено: apiVersion/kind во всех YAML-фрагментах (Pod, Deployment, Service, ConfigMap, Secret, PVC, IngressRoute, HPA, kind-config.yaml) синтаксически валидны (python3 yaml) и соответствуют актуальному Kubernetes API; сквозные имена и порты согласованы между шагами (`app: api` в Deployment/Service, `api-service`:3000 в Service/IngressRoute/ConfigMap DB_HOST, `api-config`/`api-secret` в ConfigMap/Secret/envFrom, `postgres-pvc`+`/var/lib/postgresql/data` в PVC/volumeMounts, `pg_isready -U postgres` соответствует дефолтному пользователю postgres); перекрёстные ссылки на Docker-лабу (разделы 6.1, 7.2 ×2, 8.3, 8.4) и на Traefik-лабу (раздел 5, раздел 13 с точной цитатой) — верны; раздел 7.2 ↔ шаг 1.1 (совпадающий kind-config.yaml) — согласованы; латиницы вместо кириллицы и сырых бэктиков вне code-блоков не найдено.

### Часть 2 (разделы 9–13)
- **[противоречие] Шаг 1.4** — «Каталог api/ с Dockerfile и server.js — тот же самый, что вы собрали в Traefik-лабе (раздел 4, шаг 2.1)» → в Traefik-лабе раздел 4 — это таблица «Технологический стек и структура проекта», шагов (`шаг X.Y`) там нет; реальный код `api/Dockerfile`/`server.js` — это шаг 2.1 раздела 9 «Пошаговая сборка проекта (3 сессии)» → исправить на «раздел 9, шаг 2.1».
- **[противоречие] Шаг 2.3** — «Frontend — тот же nginx:alpine-образ со статикой из Traefik-лабы (раздел 4, шаг 2.2)» → та же ошибка нумерации: frontend-код — это шаг 2.2 раздела 9 Traefik-лабы, а не раздела 4 (раздел 4 там — только таблица стека) → исправить на «раздел 9, шаг 2.2».
- **[тех] Шаг 2.2, deployment-postgres.yaml** — `envFrom` тянет `DB_NAME` из `api-config`, но, в отличие от `POSTGRES_PASSWORD` (явно замаплен через `env: valueFrom: secretKeyRef: key: DB_PASSWORD`), `POSTGRES_DB` нигде явно не задаётся — образ `postgres` переменную `DB_NAME` не понимает (только `POSTGRES_DB`/`POSTGRES_USER`/`POSTGRES_PASSWORD`) → при первом старте создастся база с именем по умолчанию (`postgres`), а не `traefik_lab` (см. также разбор в Части 1 про `DB_NAME`/`server.js` без БД) → добавить `- name: POSTGRES_DB` / `valueFrom: configMapKeyRef: {name: api-config, key: DB_NAME}` по аналогии с паролем.
- — остальное в разделах 9–13 (шаги сессий 1–3, чек-лист, глоссарий, вопросы для самопроверки, «что дальше») проверено: нумерация оглавления (часть 1) совпадает с заголовками шагов части 2; YAML всех манифестов (kind-config, Pod/Deployment/Service для whoami и api, PVC+Deployment+Service postgres, Deployment+Service adminer, Deployment traefik, IngressRoute, HPA) синтаксически корректны (`python3 yaml.safe_load_all`), apiVersion и поля соответствуют актуальному Kubernetes API; лейблы/селекторы (`app: whoami|api|postgres|adminer|traefik`) и порты (containerPort↔targetPort↔servicePort↔IngressRoute, включая `/health` в readinessProbe/livenessProbe и в `curl http://api.localhost/health`) согласованы по всем шагам; имена ConfigMap/Secret (`api-config`/`api-secret`) одинаковы в шагах 2.1–2.2; имя кластера `k8s-lab` и образы `traefik-lab-api:v1`/`traefik-lab-frontend:v1` согласованы между `kind create`/`docker build`/`kind load`/манифестами; `nodeSelector: ingress-ready: "true"` и toleration на `node-role.kubernetes.io/control-plane` в Deployment traefik соответствуют `kind-config.yaml` (шаг 1.1); metrics-server (шаг 3.3) ставится с последующим патчем `--kubelet-insecure-tls` (в kind он действительно нужен — на месте), HPA (шаг 3.3) опирается на `resources.requests`, заданные в шаге 2.5 (заданы); чек-лист (раздел 10), глоссарий (11) и вопросы для самопроверки (12) соответствуют телу методички, ничего лишнего/неопределённого не нашёл; дорожная карта (13) не противоречит списку «что не входит» из раздела 0 (Helm/StatefulSet/Operators/multi-node/service mesh/GitOps вынесены туда согласованно); остальные ссылки «см. раздел N» внутри части 2 (свои — 2.2, 3.2, 7.2, 8; на Traefik-лабу — раздел 6, раздел 8) — верны.

### ✍️ Решение по вычитке
- [ ] ⭐ A — исправить всё из вычитки выше
- [ ] B — исправить только [тех] и [противоречие]
- [ ] C — выборочно (отметь пункты выше)
