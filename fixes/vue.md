СВЯЗИ С NestJS не будет только то что в работе дан как пример для рабочего сервиса!
# Vue Lab — находки

Методичка: `vue/Vue_Lab_Helpdesk.html`. Битых якорей в оглавлении нет.
README, карточка на сайте и методичка согласованы: Vue 3.5, Vite, Pinia, Vue Router 4, Vitest, бэкенд NestJS дан готовым.

## 🔴 Связь с NestJS Lab
Фронт этой лабы **не совместим** с бэкендом из NestJS Lab (роли, статусы, логин, WebSocket — всё разное).
Подробная таблица — в `fixes/nestjs.md`. Сама Vue-лаба в этом не виновата: у неё свой готовый мини-бэкенд
(`server/src/data.ts`), и с ним всё сходится. Правка нужна в NestJS-лабе и корневом README.

## 🟡 Версии (не ошибки, а «устаревает»)
- В таблице стека: **Vite 6, Pinia 2**. На сентябрь 2026 актуальны Vite 7 (с июня 2025) и Pinia 3 (с февраля 2025).
  `npm create vue@latest` поставит свежие версии → в `package.json` будут не те цифры, что в методичке.
  API, которым пользуется лаба (`defineStore`, `storeToRefs`, плагины), в Pinia 3 не изменился — править код не надо,
  достаточно обновить таблицу или написать «Vite 6+ / Pinia 2+».
- Образ `node:22` — работает; Node 24 — текущий активный LTS.

## 🟢 Проверено и в порядке
- `persist: ['token', 'user']` — это **свой** 12-строчный плагин лабы, а не `pinia-plugin-persistedstate`
  (у того синтаксис другой). Всё корректно, и в тексте это объяснено.
- `defineModel` помечен как «Vue 3.4+» — верно.
- Порядок сессий «Pinia раньше Router, потому что guard'ам нужен auth-store» — логичен и выдержан.

## 🟡 Мелочь
- `docker-compose.yml` в репозитории лабы — заглушка `services:` без сервисов.


---

## ✍️ Варианты решений

Оставь в каждом вопросе **один** вариант (остальные удали или поставь `[x]` у нужного).
Потом я прочитаю этот файл и перепишу лабу ровно по выбранному. ⭐ — моя рекомендация.

### 1. Упоминание NestJS в Vue Lab (ты написал: «связи с NestJS не будет»). Сейчас готовый мини-бэкенд лабы написан на NestJS, и на карточке в стеке стоит чип «NestJS»
- [ ] ⭐ A — мини-бэкенд оставить, но на карточке/в README писать «готовый мини-бэкенд (Node)» вместо «NestJS», и одной фразой в методичке: «это не NestJS Lab, бэкенд дан только чтобы было к чему ходить»
- [ ] B — переписать готовый бэкенд на Express или json-server, чтобы слово NestJS в Vue Lab не встречалось вообще (заметная переделка сессии 1)
- [ ] C — оставить как есть

### 2. Версии в таблице стека: Vite 6, Pinia 2 (актуальные — Vite 7, Pinia 3)
- [ ] ⭐ A — писать «Vite 6+ / Pinia 2+», код не трогать (API тот же)
- [ ] B — обновить до Vite 7 / Pinia 3 и перепроверить все команды установки
- [ ] C — оставить

### 3. Версия Node
- [ ] ⭐ A — как решишь в `site.md` (вопрос 2)
- [ ] B — отдельно для этой лабы: оставить 22

---

## 📖 Вычитка методички

### Часть 1 (разделы 1–7, до «7. Данные: API-слой…»)
- **[тех] Раздел 6 «Pinia — общее состояние», пример стора** — «async function load(params) { items.value = await api.tickets.list(params) }» → `api.tickets.list` (как и `ticketsApi.list` из раздела 7/шага 1.5) возвращает пагинированный объект `{ items, total, page, limit }`, а не массив; `items.value` станет этим объектом, и соседние `byId`/`open` (`items.value.map(...)`, `items.value.filter(...)`) упадут с ошибкой — у объекта нет `.map`/`.filter`. Правильный паттерн распаковки уже показан в шаге 1.5 (`page.value?.items ?? []`) → нужно `items.value = (await api.tickets.list(params)).items` (или аналогично распаковать).
- **[противоречие] Раздел 5, блок «🔬 Под капотом: почему фильтры — в query, а не в ref»** — «Это делает composable useRouteQuery в сессии 3» → composable `useRouteQuery` в самом деле создаётся в Шаге 4.4 «useRouteQuery: фильтры в URL через writable computed» (Сессия 4, Vue Router), а не в сессии 3 (Pinia). Исправить на «в сессии 4».
- **[тех] Раздел 3, таблица «Симптом / Причина / Что делать»** — строка «watch(obj, cb) не срабатывает на obj.field = x» / «для ref-объекта watch по умолчанию не глубокий» / «{ deep: true } или watch(() => obj.value.field, cb)» → раз obj — ref-объект (как явно указано в колонке «Причина»), в колонке «Симптом» и в исправлении должна быть одна и та же запись `obj.value.field = x`, а не `obj.field = x` без `.value`.
- — остальное в разделах 1–7 (введение, азбука Vue 2.1–2.14, реактивность под капотом, компонентная архитектура, Vue Router) проверено, расхождений не найдено.

### Часть 2 (окончание раздела 7, раздел 8, раздел 9 — Сессия 1, шаги 1.1–1.2 и начало 1.3)
- **[противоречие] Раздел 7 «Данные: API-слой, загрузка, ошибки, WebSocket»** — «Оптимистичное обновление (сессия 4): показать изменение сразу, откатить при ошибке — это работа store» → по оглавлению (пункт 3.3 «Оптимистичное обновление») и по самому Шагу 3.3 дальше в методичке оптимистичное обновление — тема сессии 3 (Pinia), а не сессии 4. Исправить на «(сессия 3)».
- **[противоречие] Раздел 8, дерево структуры проекта** — «├── ui/ BaseButton, BaseInput (defineModel), BaseModal (Teleport+slots), StatusBadge» → компонент `BaseButton` нигде в методичке не создаётся и не используется ни разу; при этом `BaseSelect` (создаётся в Шаге 2.2, используется в `FilterBar` и позже в форме тикета) в этот список не попал. Итоговая архитектурная схема дальше по документу верно перечисляет `BaseSelect` и не содержит `BaseButton` — этот ранний список рассогласован с фактическим набором компонентов.
- **[тех] Шаг 1.2, «ожидаемый результат»** — «localhost:5173 показывает пустую страницу без ошибок в консоли.» → на этом шаге `App.vue` (написан в шаге 1.1) уже импортирует `./views/PlaygroundView.vue`, но этот файл был удалён командой `rm -rf web/src/views/*` в том же шаге 1.1 и создаётся только в шаге 1.3. На момент проверки после шага 1.2 Vite должен показать ошибку разрешения импорта («Failed to resolve import»), а не пустую страницу без ошибок.
- — остальное в разделах 7–9 (стек, структура проекта, поток данных, обзор пяти сессий, бэкенд NestJS шага 1.2 целиком) проверено, расхождений не найдено.

### Часть 3 (Сессия 1: окончание шага 1.3, шаги 1.4–1.5 — Сессия 2: шаги 2.1 и начало 2.2)
- **[противоречие] Шаг 1.3, «ожидаемый результат», первый пункт** — «count и double растут, plain в шаблоне остаётся 0 — хотя в памяти растёт. … plain обновится заодно, но никогда — сам по себе.» → внутри одного пункта два противоречащих утверждения: сначала «plain в шаблоне остаётся 0», затем «plain обновится заодно» (это же подтверждает и «Зачем» ниже: «plain обновился «заодно»»). Верно второе: при перерисовке, вызванной изменением count, шаблон читает актуальное значение plain и покажет обновлённое число — первую фразу («остаётся 0») нужно убрать или переписать, иначе результат сам себе противоречит.
- — остальное в шагах 1.3 (код песочницы и вопросы «проверь себя»), 1.4 (списки/фильтры/ключи), 1.5 (API-слой, useAsync) и в начале Сессии 2 (StatusBadge/TicketCard/TicketList, defineModel/BaseInput/BaseSelect/FilterBar/useDebounce) проверено, расхождений не найдено.

### Часть 6 (Сессия 4: шаги 4.3–4.4; Сессия 5: шаги 5.1–5.2 и начало 5.3)
- **[противоречие] Шаг 4.3 (TicketCard.vue) vs Шаг 5.3 (TicketCard.spec.js)** — в шаге 4.3 методичка меняет карточку: «TicketCard.vue — карточка ведёт на страницу: emit('select') заменяем ссылкой» (заголовок в тексте перед сниппетом с `<RouterLink>`), и модалку выбора в TicketsListView предлагается удалить («модалку с описанием можно удалить: у тикета теперь своя страница»), то есть событие `select` перестаёт быть нужным. Но тест из шага 5.3 (`web/src/__tests__/TicketCard.spec.js`) всё ещё проверяет именно старый механизм этого события: `expect(w.emitted('select')).toBeUndefined() // .stop на select сработал`. Комментарий ссылается на `@click.stop` у `<select>`, который в исходной версии компонента (сессия 2.1) не даёт клику по выпадающему списку статуса всплыть до `@click="emit('select', ticket.id)"` на `<article>`. Если после 4.3 `emit('select')` и сам `@click` на `<article>` действительно убраны (как буквально сказано в 4.3), то assert `toBeUndefined()` в тесте 5.3 проходит не благодаря `.stop`, а просто потому что такого emit'а больше не существует — комментарий в тесте вводит в заблуждение. Если же `emit('select')`/`@click` на самом деле не убирались (диф в 4.3 показывает только замену `<h3>` на `<RouterLink>`, а не полную версию файла), тогда неясно, зачем убирать модалку в TicketsListView, но оставлять бесполезный `select`, и `TicketList.vue` продолжает релеить `@select="emit('select', $event)"` в никуда. → нужно либо показать полный итоговый `TicketCard.vue` после 4.3 (с явным решением судьбы `defineEmits(['select', ...])` и `@click` на `<article>`), либо поправить комментарий в тесте 5.3, чтобы он не ссылался на устаревшее поведение `.stop`.
- — остальное в шагах 4.3–4.4 и 5.1–5.2 (WebSocket useSocket, гонка сокет/оптимистика, канбан TransitionGroup/component :is/defineAsyncComponent, useRouteQuery) проверено технически (код, API Vue 3.5/Pinia/Vue Router, соответствие ожидаемым результатам) — расхождений не найдено.

### Часть 7 (окончание шага 5.3: тесты composable/store/guard, шаг 5.4, разделы 10–13)
- — проверено: тесты Vitest/Vue Test Utils (fake timers, vi.mock, createMemoryHistory), production-сборка и nginx-конфиг, таблица «до/после», глоссарий (в т.ч. useTemplateRef — API Vue 3.5), раздел вопросов на понимание и подборка «что дальше» — технических ошибок, противоречий с оглавлением/заголовками и нарушений разметки не найдено.

### Часть 4 (шаги 2.2 хвост – 3.2, файл part_04.txt)
- **[противоречие] Структура проекта (раздел 8) vs Шаг 3.2** — дерево проекта даёт «main.js createApp → use(router) → use(pinia) → mount», а код в Шаг 3.2 (`createApp(App).use(pinia).use(router).mount('#app')`) и разбор «под капотом» в Шаг 4.2 («вот зачем Pinia была раньше роутера») говорят об обратном порядке (`pinia` до `router`). Исправить порядок в дереве проекта (раздел 8) на `use(pinia) → use(router)`, чтобы не противоречить коду сессий 3–4.
- — остальное в шагах 2.2 (хвост)–3.2 проверено: FilterBar/defineModel, BaseModal/Teleport/provide-inject, useNow/RelativeTime, ticketsStore, начало authStore — код и объяснения согласованы, расхождений не найдено.

### Часть 5 (шаги 3.2 хвост – 4.3, файл part_05.txt)
- **[текст] Шаг 3.3, заголовок «под капотом»** — «🔬 Под капотом: 4 вместо 403 — намеренная неаккуратность бэкенда» → пропущен ноль: должно быть «404 вместо 403» (в тексте ниже explicitly говорится про `NotFoundException`/404 vs правильный 403). Исправить заголовок на «404 вместо 403».
- **[тех] Шаг 4.1, «под капотом: RouterLink активные классы»** — «`router-link-active` — путь «включает» ссылку (`/tickets` активна на `/tickets/5`)» → в маршрутах этого приложения (Шаг 4.1) `/tickets` и `/tickets/:id(\d+)` объявлены как два самостоятельных top-level маршрута (не родитель/потомок), поэтому `matched` для `/tickets/5` не включает запись `/tickets` — ссылка «Список» не получит класс `router-link-active` на странице тикета, вопреки утверждению текста. Либо привести другой (реально вложенный) пример, либо явно оговорить, что для этого нужно быть предком в дереве маршрутов.
- **[тех] Шаг 3.3, сценарий 1 (не залогинены → смена статуса)** — «бейдж меняется мгновенно и через 300 мс возвращается» → `AuthGuard.canActivate` на бэкенде синхронный и сразу бросает `UnauthorizedException` (без `await sleep()`, который вызывается только внутри `TicketsService.update`, до которого guard не пропускает неавторизованный запрос), поэтому откат произойдёт практически мгновенно, а не спустя заметные ~300 мс, как в остальных (авторизованных) сценариях. Стоит поправить формулировку (откат почти сразу) или пояснить разницу с авторизованными запросами.
- — остальное в шагах 3.2 (хвост)–4.3 проверено (authStore/persist, LoginForm/AppHeader, changeStatus + откат, uiStore и сравнение ref/provide-store, роуты/lazy/404, guards, вложенные вкладки/TicketForm/NewTicketView) — код, тексты сценариев и перекрёстные ссылки (roли anna/boris, ApiError.status, byStatus для канбана) согласованы, расхождений не найдено.

### ✍️ Решение по вычитке
- [ ] ⭐ A — исправить всё из вычитки выше
- [ ] B — исправить только [тех] и [противоречие]
- [ ] C — выборочно (отметь пункты выше)
