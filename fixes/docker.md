# Docker Lab — находки

Методичка: `docker/Docker_Bash_Lab.html`. Битых якорей в оглавлении нет.

## ✅ Исправлено сразу
- На сайте (карточка `index.html` и страница `works/docker.html` через `works/lab.js`) сложность была **«—»**.
  В README лабы — «базовая по входу, но объёмная». Поставил **«Базовая»**.

## 🟡 Несостыковка «3 сессии»
Корневой README: «Что внутри (3 сессии)». README самой лабы деления на сессии не даёт, а описывает блоки
(разбор с нуля, образы и Dockerfile, Bash, тома и сети, Compose). Проверь по оглавлению методички
и выровняй формулировку в корневом README.

## 🟢 В порядке
- Связь с Traefik описана честно: Docker-лаба — подробная версия вводного раздела Traefik-лабы
  и заканчивается «точкой возврата к Traefik Lab». Порядок Docker (1) → Traefik (4) верный.
- Используется `docker compose` (v2), а не устаревший `docker-compose`; нет устаревшего поля `version:` в compose-файлах.
- Образ `postgres:16-alpine` (Kubernetes-лаба использует тот же) — ок.


---

## ✍️ Варианты решений

Оставь в каждом вопросе **один** вариант (остальные удали или поставь `[x]` у нужного).
Потом я прочитаю этот файл и перепишу лабу ровно по выбранному. ⭐ — моя рекомендация.

### 1. В корневом README «Что внутри (3 сессии)», а в README лабы деления на сессии нет
- [ ] ⭐ A — я сверю оглавление методички и выровняю формулировку в корневом README
- [ ] B — оставить

### 2. Сложность «Базовая» (уже поправил вместо «—»)
- [ ] ⭐ A — оставить «Базовая»
- [ ] B — написать «Базовая, объёмная», как в README

---

## 📖 Вычитка методички

### Часть 1 (разделы 0–4, до середины 4.4)
- **[противоречие] Раздел 0, «КАК ПОЛЬЗОВАТЬСЯ»** — «entrypoint-скрипты в разделе 5 и в **сессии 9** написаны на нём» (ссылка ведёт на `#sec-9`) → раздела «сессия 9» не существует: раздел 9 называется «Пошаговая сборка проекта (3 сессии)» и сам состоит из сессий 1–3 → исправить на «в разделе 9».
- **[противоречие] Раздел 1.2, блок «🔬 Под капотом»** — «cgroups… флагами вроде `--memory` и `--cpus`, которые встретятся **в разделе 3.3**» (ссылка ведёт на `#step-3-3`, т.е. на Шаг 3.3 «restart policies и лимиты ресурсов») → в тексте это подписано как «раздел 3.3», а раздел 3.3 в этом же документе — совсем другая тема («Build context и .dockerignore»); текст должен ссылаться на «шаг 3.3».
- **[противоречие] Раздел 2.1, комментарий к `docker stop web`** — «мягкая остановка (SIGTERM, потом SIGKILL — см. раздел 5)» → раздел 5 только вскользь упоминает PID 1 и сигналы, а конкретный механизм SIGTERM→(10с)→SIGKILL подробно раскрыт в шаге 2.4 (на который в остальном тексте лабы всегда и ссылаются, см. строки 579/622/904) → исправить на «см. шаг 2.4».
- — остальное в разделах 0–4 (до середины 4.4) проверено технически (bash-конструкции, таблицы exit-кодов, restart policy, слои/кэш Dockerfile, alpine/slim/distroless) — расхождений не найдено.

### Часть 2 (разделы 4.4–9, до Шага 2.1 включительно)
- **[противоречие] Раздел 5.3, блок «💡 exec — это не "запустить", а "стать"»** — «без неё сигналы остановки контейнера (**раздел 2.4**) будут приходить в bash» (ссылка ведёт на `#step-2-4`) → раздела «2.4» у раздела 2 «Жизненный цикл контейнера» вообще нет (там только 2.1–2.3: команды, отладка, restart policy), «2.4» существует только как Шаг 2.4 в разделе 9 (сессия 2, «PID 1, сигналы и trap») → исправить на «шаге 2.4».
- **[противоречие] Шаг 1.1, плашка «УЖЕ В РЕПО»** — «Папка `app/` и **`docs/`** уже созданы пустыми (заглушки `.gitkeep`)» → в дереве проекта этого же шага папки `docs/` нет вообще (только `docker-compose.yml`, `.env`, `.env.example`, `app/…`) — похоже на копипасту из другой лабы → убрать упоминание `docs/` или добавить эту папку в структуру, если она нужна.
- — остальное в разделах 4.4 (после «until»)–8 и в шагах 1.1–2.1 сессий 1–2 (bash-конструкции, ENTRYPOINT/CMD, volumes, сети, Compose, весь код Node.js/Dockerfile/entrypoint.sh) проверено, в том числе прогоном `bash -n` и выполнением сниппетов локально — расхождений не найдено.

### Часть 3 (Шаг 2.2 — раздел 13, конец методички)
- **[противоречие] Шаг 2.3, блок «🐚 BASH»** — «Цикл `until (...) 2>/dev/null; do ... done` — конструкция из раздела 4.4: "повторять, пока условие ложно"» (ссылка ведёт на `#sec-4`) → в самом разделе 4.4 тот же `until`-цикл подписан комментарием `# цикл "пока условие истинно"` — прямо противоположная и семантически неверная для `until` формулировка (`until` повторяет тело, пока условие **ложно**, и останавливается, когда оно становится истинным) → унифицировать: исправить комментарий в разделе 4.4 на «пока условие ложно», чтобы он совпадал с корректным описанием в шаге 2.3.
- **[противоречие] Шаг 3.4, вводный абзац** — «отлаживая по слоям — build → run → network → volume → entrypoint, как советует раздел 2.2» (ссылка на `#sec-2`) → раздел 2.2 «Отладка живого контейнера» описывает только команды `docker logs`/`docker exec`/`docker inspect`/`docker stats`, никакой рекомендации по порядку отладки «build → run → network → volume → entrypoint» там нет, и эта последовательность больше нигде в методичке не встречается → либо убрать ссылку на раздел 2.2, либо перенести туда эту рекомендацию, либо сослаться на подходящий раздел/шаг.
- **[текст] Шаг 3.4, «🔬 Разбор», пункт 1 «Порт наружу»** — «приложение слушает `3000` внутри, а проброшен `8080:внутри`» → фраза оборвана/бессмысленна, `8080:внутри` не является ни валидной записью проброса порта, ни читаемым русским текстом → переформулировать, например: «...а проброшен как `3000:8080` (хост:контейнер), то есть снаружи открыт правильный порт 3000, но Docker пересылает его на несуществующий внутри контейнера порт 8080».
- — остальное в шагах 2.2–3.4 и разделах 10–13 (сети/DNS, wait-for-postgres.sh, PID 1/trap, multi-stage build, compose.yml, .env, restart policies/лимиты, Production Hell, чек-лист, глоссарий, вопросы для самопроверки, дорожная карта) проверено, в том числе прогоном `bash -n` по всем bash-сниппетам и разбором YAML синтаксическим парсером — расхождений и технических ошибок не найдено.

### ✍️ Решение по вычитке
- [ ] ⭐ A — исправить всё из вычитки выше
- [ ] B — исправить только [тех] и [противоречие]
- [ ] C — выборочно (отметь пункты выше)
