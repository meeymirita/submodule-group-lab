# Traefik Lab — находки

Методичка: `traefik/Docker_and_Traefik_Lab_Plan.html`. Битых якорей в оглавлении нет.
README, карточка и методичка согласованы: Traefik 3, Docker Compose, Node.js API, PostgreSQL, Adminer.

## 🟢 Синтаксис Traefik v3 — корректный
Проверил типичные ловушки миграции v2 → v3: нет `ipWhiteList` (в v3 — `ipAllowList`), нет старого синтаксиса
`HostRegexp(`{...}`)`, нет удалённого `swarmMode`, нет старой группы CRD `traefik.containo.us`. Образ закреплён: `traefik:v3.1`.
Упоминание «Traefik 2» — только в историческом контексте.

## 🟡 На эту лабу жёстко опирается Kubernetes
Kubernetes-лаба копирует отсюда `api/` (Dockerfile + server.js) и собирает образ `traefik-lab-api:v1`.
Значит, при правках API здесь нужно проверять шаги 1.4+ в Kubernetes-лабе. Имеет смысл добавить в конец
этой методички строчку «Не удаляй папку `api/` — она понадобится в Kubernetes Lab».

## 🟡 Мелочи
- `traefik:v3.1` — рабочая, но уже не последняя ветка v3. Обновлять — вместе с Kubernetes-лабой (там тоже v3.1).
- `postgres:16` здесь, `postgres:16-alpine` в Kubernetes-лабе — см. `fixes/kubernetes.md`.
- `docker-compose.yml` в репозитории — заглушка `services:`; `traefik/acme/acme.json` уже лежит пустым — хорошо
  (Traefik требует этот файл с правами 600, стоит напомнить `chmod 600` в шаге с Let's Encrypt, если ещё нет).


---

## ✍️ Варианты решений

Оставь в каждом вопросе **один** вариант (остальные удали или поставь `[x]` у нужного).
Потом я прочитаю этот файл и перепишу лабу ровно по выбранному. ⭐ — моя рекомендация.

### 1. Kubernetes-лаба берёт отсюда `api/`
- [x] ⭐ A — добавить в конец методички строчку «не удаляй `api/`, она понадобится в Kubernetes Lab»
- [ ] B — не трогать

### 2. Traefik `v3.1`
- [x] ⭐ A — как решишь в `kubernetes.md` (вопрос 4), обновлять только вместе
- [ ] B — отдельно: обновить только здесь

### 3. `chmod 600 acme.json` перед Let's Encrypt
- [x] ⭐ A — проверить шаг и добавить напоминание, если его нет
  - 💬 вычитка подтвердила: в репо `acme.json` с правами 644, а git 600 не хранит — `chmod 600` сделать обязательным шагом, фразу «можно пропустить» убрать.
- [ ] B — не трогать

### 4. PostgreSQL 16
- [x] ⭐ A — как решишь в `site.md` (вопрос 1)
- [ ] B — отдельно: оставить 16

---

## 📖 Вычитка методички

### Часть 1 (разделы 0–7)
- **[текст] Раздел 6 «Сценарий: путь запроса», раздел 7 (таблица Middlewares)** — в HTML-источнике буквально `<code>Host(\`api.localhost\`)</code>` и `<code>PathPrefix(\`/app\`)</code>` (лишний обратный слэш перед каждым бэктиком) → в браузере вместо `Host(`api.localhost`)` читатель видит `Host(\`api.localhost\`)` с видимыми слэшами → убрать экранирующие `\` перед бэктиками (как это верно сделано в блоке `<pre>` раздела 5 с примером whoami).
- **[противоречие] Раздел 1 «Разбор задачи», таблица «Что доработано», строка «Один Docker provider на всё»** — «раздел 5 объясняет providers и когда нужен file provider вместо labels» (ссылка `href="#sec-5"`) → объяснение двух видов providers (Docker vs file) и их merge на самом деле находится в разделе 2, в блоке «🔬 Под капотом: почему providers два вида»; раздел 5 («Service Discovery через labels») про file provider вообще не говорит, только про Docker-labels на примере whoami → исправить ссылку на раздел 2 (`#sec-2`).
- **[противоречие] Раздел 0.5 «Сеть», врезка «ЭТО КЛЮЧЕВАЯ ИДЕЯ ДЛЯ ВСЕЙ ЛАБЫ»** — «когда в разделе 5 вы увидите `traefik.http.services.api.loadbalancer.server.port=3000`» (ссылка `#sec-5`) → в разделе 5 показан только пример `whoami` с `port=80`; label `api` с `port=3000` появляется только в шаге 2.1 (Сессия 2), которого на момент раздела 5 в тексте ещё не было → сослаться на шаг 2.1, а не на раздел 5.
- **[противоречие] Раздел 3 «Итоговая архитектура», абзац после ASCII-диаграммы** — «PostgreSQL и API дополнительно используют внутреннюю сеть `backend`, недоступную из `traefik-public` — Traefik физически не может замаршрутизировать трафик напрямую в БД, только через API или Adminer» → если в сети `backend` по тексту только `PostgreSQL` и `api`, то `adminer` до БД достучаться не может вообще, а фраза тут же утверждает обратное («через API или Adminer»); по факту (см. шаг 2.3, сервис adminer) `adminer` тоже подключён к `traefik-public` и `backend` → добавить `adminer` в перечисление сервисов сети `backend`.
- — остальное в разделах 0–7 проверено, расхождений не найдено: соответствие оглавления (сайдбар и «Содержание») заголовкам `id="sec-0"`…`id="sec-8"` и их порядок; последовательность сносок-глоссария 1–10 (Traefik, TLS, статическая/динамическая конфигурация, EntryPoint, Provider, Router, Middleware, Service, round robin) без пропусков и дублей; перекрёстные ссылки «шаг N.N» на шаги 1.2, 1.3, 1.5, 1.6, 2.2, 2.3, 2.4, 2.5, 3.1, 3.2 — совпадают с оглавлением сессий; команды `docker pull/build/run/ps/logs/exec/stop/rm` и `docker compose up/ps/logs/down` (блоки 1–2 pre_blocks.txt) — синтаксис и комментарии верны; ASCII-диаграммы разделов 2 и 3 (блоки 3–4) и дерево каталогов раздела 4 (блок 5) внутренне согласованы (imена сетей `traefik-public`/`backend`, сервисов, портов 80/443) с кодом более поздних шагов (2.1, 2.3, session 3); пример label whoami (блок 6, раздел 5) — синтаксис Traefik v3 корректен (`Host(...)` с бэктиками, `entrypoints=web`, нет устаревшего v2-синтаксиса); латиница вместо кириллицы в прозе разделов 0–7 не найдена (проверено скриптом по символьным паттернам); сырые бэктики вне `<code>`/`<pre>` в разделах 0–7 не найдены.

### Часть 2 (разделы 8–9: Сессии 1–2 и начало Сессии 3)
- **[тех] Шаг 1.1 «Создаём каталоги»** — «и `traefik/acme/acme.json` уже существует с правами `600` — эту команду можно пропустить целиком» → в реальном репозитории `traefik/traefik/acme/acme.json` имеет права `644` (`rw-r--r--`, проверено `stat`/`ls -la`), а не `600`; к тому же git не хранит полные unix-права (кроме бита исполняемости), поэтому после любого чекаута заявленные 600 почти всегда слетают до дефолтных 644 — утверждение структурно ненадёжно, не только фактически неверно сейчас → убрать фразу «уже с правами 600» либо всё равно явно попросить выполнить `chmod 600 traefik/acme/acme.json` перед шагом 3.2 (Let's Encrypt), не полагаясь на состояние репозитория.
- **[противоречие] Шаг 1.1** — команда `mkdir -p traefik-lab/{traefik/dynamic,traefik/acme,api,frontend,db}` + `cd traefik-lab` → note-box в этом же шаге утверждает, что каталоги `api/`, `frontend/`, `db/`, `traefik/dynamic/`, `traefik/acme/` уже созданы «в репо» (на верхнем уровне, без обёртки `traefik-lab/`), а сама команда, если её выполнить, создаёт НОВУЮ вложенную папку `traefik-lab/` со своими `traefik/dynamic`, `traefik/acme` и т.д. — то есть не трогает существующие каталоги и существующий `acme.json`, а плодит дублирующую структуру рядом → либо убрать префикс `traefik-lab/` и `cd traefik-lab` из команды (раз каталоги и так уже на месте в корне проекта), либо явно объяснить, что дальше все пути в лабе (`./api`, `./frontend` и т.д.) даны от корня уже склонированного репозитория, а не от новой `traefik-lab/`.
- **[противоречие] Шаг 1.4 «Первый роутер — labels на whoami»** — «Здесь — проверка, что вы понимаете, что произошло, плюс второй роутер с явным приоритетом» → в блоке кода показан не второй роутер, а тот же самый роутер `whoami` (то же имя `traefik.http.routers.whoami.*`) с добавленной строкой `priority=1` — никакого второго роутера в примере нет → либо убрать слова «плюс второй роутер», заменив на «плюс приоритет у того же роутера», либо реально добавить второй пересекающийся роутер (как в примере из «Зачем») для демонстрации приоритета.
- **[текст] Шаг 1.4, блок «Зачем»** — `<code>Host(\`app.localhost\`)</code> и <code>Host(\`app.localhost\`) && PathPrefix(\`/api\`)</code>` → в HTML внутри `<code>` буквально остались экранированные бэктики `\`` (обратный слэш перед бэктиком), в браузере это отрисуется как видимые символы `\` рядом с примером правила, а не как чистый код → убрать лишние `\` перед бэктиками (должно быть просто `` Host(`app.localhost`) ``).
- **[тех] Шаг 1.5 «Dashboard и его защита»** — «без него label `api.dashboard=true` из шага 1.2 включает API» → в шаге 1.2 `api: dashboard: true` задаётся в статической конфигурации `traefik/traefik.yml` (YAML), а не через Docker label — лаба сама во 2 разделе жёстко разделяет статическую конфигурацию/labels/provider'ы, и называть настройку `traefik.yml` «лейблом» — терминологическая путаница ровно в том месте, где она особенно вредна → заменить «label `api.dashboard=true`» на «опция `api.dashboard: true` из `traefik.yml` (шаг 1.2)».
- **[противоречие] конец шага 1.6 и конец шага 2.5** — `<span class="tag">ПЕРЕД РАЗДЕЛОМ <a href="#sec-9">2.1</a></span>` и `<span class="tag">ПЕРЕД РАЗДЕЛОМ <a href="#sec-9">3.1</a></span>` → во всех остальных переходах между шагами используется формат «ПЕРЕД ШАГОМ X.Y» простым текстом без ссылки (см. «ПЕРЕД ШАГОМ 1.4», «ПЕРЕД ШАГОМ 2.2» и т.д.), а здесь — слово «РАЗДЕЛОМ» вместо «ШАГОМ» и ссылка `#sec-9`, которая ведёт на заголовок раздела 9 целиком, а не на сам шаг 2.1/3.1 → исправить на `ПЕРЕД ШАГОМ 2.1` и `ПЕРЕД ШАГОМ 3.1` без ссылки (или со ссылкой на `#step-2-1` / `#step-3-1` соответственно), по аналогии с остальными шагами.
- **[тех] Шаг 2.1 «Backend API»** — `api/Dockerfile` делает `COPY package.json .` и `RUN npm install`, а `server.js` содержит `require('express')` → содержимое `api/package.json` (с зависимостью `express`) нигде в методичке не приведено — ни в разделе 4 (структура проекта), ни в шаге 2.1; в самом репозитории `traefik/api/` содержит только `.gitkeep`, файла нет вовсе → без `package.json` с `express` в зависимостях `npm install` ничего не поставит и `require('express')` упадёт при старте контейнера → добавить в шаг 2.1 блок с содержимым `api/package.json` (минимум `{"dependencies": {"express": "^4"}}`).
- **[тех] Шаг 2.2 «Frontend — path routing + StripPrefix»** — сервис `frontend: build: ./frontend` → нигде в методичке не приведено содержимое `frontend/Dockerfile` (только упоминание в дереве структуры раздела 4 и путь в `build:`), хотя текст шага прямо утверждает «nginx внутри контейнера…» — то есть подразумевает конкретный образ, который читателю никогда не показали; в репозитории `traefik/frontend/` тоже только `.gitkeep` → добавить блок с `frontend/Dockerfile` (`FROM nginx:alpine` + `COPY dist/ /usr/share/nginx/html/` или аналог).
- **[тех] Шаг 2.5 «Цепочка middlewares»** — «Порядок в списке — сначала лимит, потом заголовки, см. врезку в разделе 7» → врезка «🔬 Под капотом: почему порядок middlewares в цепочке важен» (раздел 7) прямо говорит обратное: «Если поставить headers (добавляет CORS) после ratelimit — заблокированный по лимиту запрос получит ответ 429 без CORS-заголовков… Правило: middlewares, которые должны сработать на «отказных» ответах тоже — ставить первыми», т.е. `secure-headers` должен идти ПЕРЕД `api-ratelimit`, а не после — а в лейбле стоит именно `traefik.http.routers.api.middlewares=api-ratelimit,secure-headers` (лимит первым) — шаг делает ровно то, от чего предостерегает собственная врезка того же документа → поменять порядок на `secure-headers,api-ratelimit` либо переписать объяснение врезки/шага так, чтобы они не противоречили друг другу.
- **[тех] Шаг 3.3 «Canary — weighted round robin»** — новый роутер `api-canary` (`traefik/dynamic/canary.yml`) с `rule: "Host(\`api.localhost\`)"` на entrypoint `web` → это тот же самый Host-паттерн и entrypoint, что и у роутера `api`, созданного labels'ами ещё в шаге 2.1 и нигде впоследствии не отключённого/не переименованного — на этом шаге в конфигурации одновременно существуют два роутера с идентичным правилом без явного `priority`, что даёт неоднозначное совпадение вместо гарантированного canary-распределения → явно указать в шаге, что labels роутера `api` нужно убрать/закомментировать (или задать разные `priority`) при переходе на `api-canary`.
- — остальное в разделах 8–12 проверено (таблица mkcert vs Let's Encrypt и лимиты Let's Encrypt; синтаксис `traefik.yml`, `middlewares.yml`, `tls.yml`, `canary.yml`, acme-резолвера — валидны как YAML; `docker-compose.yml` шага 1.3 — валиден через `docker compose config`; `server.js` — валиден по `node --check`; согласованность имён сервисов/роутеров/сетей/портов между шагами 1.2–3.2 (traefik-public/backend, whoami/api/frontend/adminer/db, healthcheck path `/health`, StripPrefix-префикс `/app`); отсутствие `container_name`/фиксированных портов у `api` для совместимости со `--scale`; нумерация сносок 10–12 (round robin/health check/forwardAuth) в разделе 11 совпадает со сносками по тексту; вопросы раздела 12, присутствующие в этой части), расхождений, кроме перечисленных выше, не найдено.

### Часть 3 (разделы 9.3–14)
- **[тех] Шаг 3.3 «Canary — weighted round robin», `traefik/dynamic/canary.yml`** — «`weighted: services: - name: api` / `- name: api-v2`» → сервис объявлен в **file**-провайдере, а `api`/`api-v2` созданы через Docker-labels (**docker**-провайдер); без суффикса `@docker` Traefik ищет `api@file`/`api-v2@file`, которых не существует — weighted-сервис не находит бэкенды, canary не работает. Тут же рядом методичка сама показывает правильный синтаксис (`service=api@internal` в шаге 1.5), но в canary.yml его не применяет → исправить на `name: api@docker` и `name: api-v2@docker`.
- **[противоречие] Конец раздела 14.7 (fix-box «Что проверить, если не заработало»)** — «Раздел `3.4` («Production Hell»)» (ссылка на `#step-3-4`) → во всём документе `3.4` — это «Шаг 3.4» (см. TOC и `step-head`), разделов с дробными номерами в документе нет (разделы — это h2 0–14) → исправить на «Шаг 3.4».
- **[текст] Раздел 12, вопрос про health check** — «Что делает health check, и что произойдёт с реплик без него, если внутри неё завис процесс?» → согласование сломано («с реплик» вместо «с репликой»), из-за чего конец фразы («внутри неё») не сочетается с началом → исправить на «с репликой».
- **[тех] Шаг 3.1 (mkcert)** — после генерации сертификата и `traefik/dynamic/tls.yml` ни один роутер лабы (`whoami`, `api`, `frontend`, `adminer`, `dashboard`) не переключается на `entrypoints=websecure`, поэтому mkcert-сертификат негде фактически проверить curl/браузером внутри самой лабы (единственные упоминания `websecure` в разделе 9 — пример `api-secure` для `api.example.com`, который явно «не выполнять буквально», и раздел 14 для реального домена). В отличие от всех остальных шагов, шаг 3.1 не заканчивается проверочной командой → стоит добавить хотя бы один тестовый роутер/curl на `websecure` с mkcert-сертификатом.
- — остальное в разделах 9.3–14 проверено, расхождений не найдено: сверил таблицу mkcert vs Let's Encrypt (раздел 8) на фактическую точность (нужен ли домен, лимиты, HTTP-01/DNS-01); staging `caServer` (`acme-staging-v02.api.letsencrypt.org`) — корректный официальный URL, снятие строки `caServer` в 14.7 действительно переключает на боевой LE по умолчанию; `entryPoints.web.http.redirections` (раздел 14.5) — верный синтаксис v3 для глобального HTTP→HTTPS редиректа; чек-лист (раздел 10) и глоссарий (раздел 11, все 12 anchor-пар `term-N`/`ref-N` резолвятся) сверены с содержанием разделов 0–14 — пунктов без покрытия или битых ссылок не нашёл; вопросы для самопроверки (раздел 12) все отвечаемы по пройденному материалу; таблицы раздела 14 (14.1 схема доменов, 14.2 DNS, 14.4 было/стало, 14.6 пример frontend) взаимно согласованы (имена поддоменов, роутеров, middlewares совпадают); раздел 13 «Что дальше» — ссылки и названия проектов (Tecnativa/docker-socket-proxy, CrowdSec bouncer и т.д.) выглядят корректно; YAML-синтаксис `canary.yml` и итогового `traefik.yml` (раздел 14.5) проверен через `python3 -c "import yaml"` — валиден; собранный `docker-compose.yml` (api/api-v2/frontend/db/adminer из разделов 9 и canary) проверен через `docker compose config` — проходит без ошибок структуры.

### ✍️ Решение по вычитке
- [x] ⭐ A — исправить всё из вычитки выше
- [ ] B — исправить только [тех] и [противоречие]
- [ ] C — выборочно (отметь пункты выше)
