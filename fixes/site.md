# Общее для всех лаб и сайта — находки

## ✅ Уже сделано (не закоммичено)
- **Картинки на главной весили 31 МБ.** Каждая обложка — PNG 1254×1254 на ~2,5 МБ, а в карточке она показывается ~400px.
  Сделал WebP-превью 1200px (`works/images/thumbs/<лаба>.webp`, ~250 КБ каждая): главная теперь весит **~3,4 МБ вместо ~34 МБ**.
  Оригинальный PNG грузится только при открытии картинки на весь экран (Flip-галерея). Страницы лаб тоже используют превью.
- Заголовок корневого README перечислял 8 лаб из 13 — дописал все.
- Сложность Docker на сайте «—» → «Базовая» (см. `docker.md`).

## 🟡 Разнобой версий между лабами
| | где сейчас |
|---|---|
| PostgreSQL 16 | Redis, NestJS, Traefik, Docker, Kubernetes (RabbitMQ — не трогаю) |
| PostgreSQL 17 | Laravel, GraphQL |
| Node 22 | Vue, TypeScript, JS, NestJS, GraphQL, Laravel (фронт) |
| Redis 7 | Redis, Laravel, GraphQL |

Актуально на сентябрь 2026: PostgreSQL 18 (с сентября 2025; в нём сменился путь данных в контейнере — GraphQL-лаба об этом уже предупреждает),
Node 24 — активный LTS (22 — maintenance до апреля 2027), Redis 8 (с мая 2025). Всё текущее **работает** — вопрос только в единообразии и «свежести».

## 🟡 Пустые `docker-compose.yml` в репозиториях лаб
В redis, traefik, vue, typescript, laravel, docker, nestjs, graphql лежит `docker-compose.yml` с одной строкой `services:` —
`docker compose` на таком файле падает с ошибкой («services must be a mapping»). Плюс пустые `Makefile`. Это заготовки
«под прохождение», но выглядят как сломанные файлы.

## 🟡 Абзац про порядок в корневом README
Длинный абзац «Порядок — от простого к сложному…» содержит неверное про NestJS («собирает тот самый бэкенд из Vue-лабы»)
и не говорит, что Kubernetes требует **код** Traefik-лабы. Подробно — `_order.md`.

---

## ✍️ Варианты решений

Оставь в каждом вопросе **один** вариант (остальные удали или поставь `[x]` у нужного).
Потом я прочитаю этот файл и перепишу лабы ровно по выбранному. ⭐ — моя рекомендация.

### 1. Версия PostgreSQL во всех лабах
- [ ] ⭐ A — везде 17 (одна версия на всё; 18 пока не трогаем из-за смены пути данных)
- [ ] B — везде 18 (самая свежая; в каждой лабе добавить предупреждение про новый путь данных, как в GraphQL)
- [ ] C — оставить разнобой 16/17

### 2. Версия Node во всех лабах
- [ ] ⭐ A — писать «Node 22+ (24 LTS тоже подходит)», образы не менять
- [ ] B — везде перейти на Node 24 (образы `node:24`, таблицы стека)
- [ ] C — оставить 22

### 3. Версия Redis
- [ ] ⭐ A — оставить Redis 7 и в Redis-лабе в «Что дальше» добавить строчку про Redis 8
- [ ] B — везде перейти на Redis 8 (`redis:8-alpine`)

### 4. Пустые `docker-compose.yml` (`services:`) и пустые `Makefile`
- [ ] ⭐ A — удалить заглушки из репозиториев лаб; файлы появятся, когда начнёшь проходить (методичка всё равно их создаёт по шагам)
- [ ] B — заменить на валидную заглушку с комментарием `# заполняется в сессии 1` (`services: {}`)
- [ ] C — оставить как есть

### 5. WebP-превью обложек (уже сделано)
- [ ] ⭐ A — оставить и закоммитить
- [ ] B — откатить, вернуть PNG в карточки

### 6. Абзац про порядок в корневом README
- [ ] ⭐ A — переписать по `_order.md`: короче, без неверного про NestJS, с явным «Kubernetes требует код Traefik-лабы»
- [ ] B — оставить
