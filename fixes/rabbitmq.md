# RabbitMQ Lab — находки

Методичка: `rabbitmq/docs/RabbitMQ_Lab_Plan_v1_pro_max.html`. Стек: Laravel 13 (PHP 8.4) + PostgreSQL 16 + RabbitMQ (Management UI) + Mailpit, всё в Docker Compose.
Лаба пройдена пользователем (все 3 сессии), явных ошибок при прохождении не замечено — вычитка запрошена как дополнительная проверка методички и справочных файлов (`docs/order-path-explained.md`, `docs/self-check-answers.md`, `docs/rabbit.md`), не как реакция на найденный баг.

---

## 📖 Вычитка методички

### Часть 3 (конец шага 1.7 «Поднимаем инфраструктуру» → шаги 1.8 Миграции, 1.9 Модели, 2.1 OutboxWriter, 2.2 API OrderController, 3.1 AmqpConnectionFactory, 3.2 AbstractAmqpWorker, 3.3 OutboxRelayCommand (обрыв части — на «дубль погасит consumer.», продолжение в части 4))
- — часть проверена целиком, расхождений не найдено: сверил все код-блоки этой части с `pre_blocks.txt` (BLOCK 18–29) — текст совпадает дословно; проверил PHP-синтаксис всех классов (`OutboxWriter`, `OrderController`, `AmqpConnectionFactory`, `AbstractAmqpWorker`, `OutboxRelayCommand`) через `php -l` — ошибок нет; сверил реальные сигнатуры php-amqplib (vendor в `rabbitmq/laravel-app/vendor/php-amqplib/php-amqplib`) для `AMQPMessage::get()/has()/ack()/nack()` (в т.ч. что `get('redelivered')` корректно читается из `delivery_info`, а `get('message_id')` — из `properties`), `AMQPChannel::confirm_select()/wait_for_pending_acks()/set_nack_handler()/basic_qos()/basic_consume()`, `AbstractChannel::wait()` и конструктор `AMQPStreamConnection` — все именованные аргументы (`heartbeat`, `keepalive`, `queue`, `consumer_tag`, `no_ack`, `callback`, `timeout`, `requeue`) существуют и указаны в верном порядке; сверил имена таблиц/колонок между шагом 1.8 (миграции) и шагом 1.9 (модели: `$fillable`, `casts()`, `$timestamps=false` там, где в миграции нет `created_at/updated_at`) — везде согласовано; сверил `OutboxWriter::write()` → `OrderController::store()` → `OutboxRelayCommand` (routing_key `order.created`, `message_id`=UUID из `outbox_messages.id`, `priority` 0–10 в валидации совпадает с `x-max-priority=10` в диаграмме раздела 3) — противоречий нет; сверил количество exchange (4) и очередей (10), упомянутое в конце шага 1.7, с `definitions.json` (BLOCK 10: `orders.topic/email.retry/email.dlx/system.broadcast` — 4; `order.queue/email.queue/analytics.queue/email.retry.1-3/email.dlq/broadcast.a-c` — 10) — совпадает; сверил с `docs/order-path-explained.md` (Хопы 1–8, таблицы outbox_messages/processed_messages) — согласовано с текстом методички; класс `PCNTLHeartbeatSender`, упомянутый в «Под капотом» шага 3.1, реально существует в vendor-пакете.

### Часть 5 (Шаг 5.4 «Слой Laravel Queue» → «ПЕРЕД СЕССИЕЙ 3» → Сессия 3: шаги 6.1 «Priority с backlog», 6.2 «Fanout», 6.3 «Production Hell» → раздел 10 «Полный чек-лист концепций» → раздел 11 «Глоссарий терминов» → раздел 12 «Вопросы для самопроверки» → раздел 13 «Что дальше»; конец методички)
- **[противоречие] Раздел 11, преамбула глоссария** — «В порядке первого появления в тексте.» → фактический порядок появления сносок в HTML (по `id="ref-N"`) — 1,2,…,16,17,**21,20**,18,19 — не совпадает с заявленным и с порядком самих номеров в глоссарии (17,18,19,20,21). Конкретно: термин №17 «Binding» (`id="ref-17"`, строка 319) появляется в тексте раньше терминов №20 «Persistent message» (`id="ref-20"`, строка 324) и №21 «Durable queue» (`id="ref-21"`, строка 323) — сам «Durable queue» (21) встречается в тексте РАНЬШЕ «Persistent message» (20), хотя номер у него больше; а термин №18 «Message headers» (`id="ref-18"`, строка 334) и №19 «Graceful shutdown» (`id="ref-19"`, строка 748) оба встречаются в тексте ПОЗЖЕ, чем «Durable queue»(21) и «Persistent message»(20). То есть правильный порядок по первому появлению должен быть: …16, 17 (Binding), затем Durable queue, затем Persistent message, затем Message headers, затем Graceful shutdown → то есть последним четырём терминам присвоены номера 18,19,20,21 не в том порядке, в каком они реально встречаются (должно быть Durable queue=18, Persistent message=19, Message headers=20, Graceful shutdown=21, либо переставить фрагменты текста). → Исправить: либо перенумеровать термины 18–21 по фактическому порядку появления (Durable queue → Persistent message → Message headers → Graceful shutdown), либо убрать/смягчить фразу «в порядке первого появления в тексте», если нумерация не строго такая.
- — остальное в части проверено целиком, расхождений не найдено: сверил все названия exchange/queue/routing key/переменных окружения этого фрагмента (`system.broadcast`, `broadcast.a/b/c`, `worker:broadcast`, `lab:broadcast`, `lab:orders`, `x-max-priority`, `order.queue`/`email.queue`/`analytics.queue`, `orders.topic`, `email.retry`/`email.dlx`, `RABBITMQ_QUEUE`, `laravel.jobs`, `LAB_SIMULATE_SMTP_FAILURE`, `LAB_SLOW_CONSUMER_MS`) с `pre_blocks.txt` (везде совпадает, включая объявление `system.broadcast`/`broadcast.a-c` уже в definitions.json шага 1.2 — согласуется со ссылкой в чек-листе «Топология: 1.2, 1.7, 6.2»); код-блок шага 5.4 (`config/queue.php` → `'rabbitmq' => [...]`, `'options' => ['queue' => ['prioritize_delayed' => true]]`, `app/Jobs/SendOrderEmailJob.php` с `$tries=4`/`$backoff=[10,30,300]`) сверил дословно с реальными файлами репозитория `rabbitmq/laravel-app/config/queue.php` и с установленным пакетом `vendor/vladimir-yuldashev/laravel-queue-rabbitmq/config/rabbitmq.php` — совпадает; `$backoff=[10,30,300]` соответствует TTL retry-очередей `email.retry.1/2/3` (10000/30000/300000 мс) из `definitions.json`; фраза «в базовом классе не хватает одного instanceof — найдите, где» (шаг 6.3, задание 5) сверил с реальным `AbstractAmqpWorker.php` — метод `onFailure()` действительно не делает `instanceof`-проверки transient/permanent ошибки (просто `nack(requeue:false)`), то есть описание задания соответствует фактическому состоянию кода; фраза «понять, почему брокер «убил» соединение (см. шаг 3.1)» (шаг 6.3, задание 8) сверил с `AmqpConnectionFactory.php` — там реально задан `heartbeat: 30`, комментарий «брокер поймёт, что воркер умер, за ~60 сек» — ссылка корректна; продукт «Monitor» (stock=5) в задании 5/6 таблицы Production Hell сверил с шагом создания товаров (`Product::insert([...,'Monitor','stock'=>5])`) — это название товара, а не отсылка на документ «Monitoring», путаницы нет; все ссылки `href="#step-N-M"`/`href="#sec-N"` в таблице чек-листа (раздел 10) резолвятся на существующие `id` в документе — битых якорей нет; количество терминов глоссария (21) совпадает с фактическим числом сносок `ref-1`…`ref-21` в HTML; количество и нумерация шагов сессии 3 (6.1–6.3) совпадает с оглавлением (часть 1) и с session-banner «СЕССИЯ 3 — Priority, Fanout, Production Hell ~3 часа»; проверил на латиницу-вместо-кириллицы и «сырые» бэктики вместо `<code>` в этом фрагменте HTML — не найдено; ссылки раздела 13 («что дальше») на `microservices.io/saga`, `docs/quorum-queues`, `docs/streams`, `docs/parameters#policies`, `docs/consumers#single-active-consumer`, плагины consistent-hash и delayed-message — реальные валидные URL (расхождений между текстом ссылки и `href` нет, кроме намеренно укороченных подписей).

### Часть 2 (конец раздела 5 «Под капотом: outbox» → раздел 6 «Сценарий: жизненный цикл заказа» → раздел 7 «Retry / DLX / DLQ» → раздел 8 «Priority, Prefetch, Competing Consumers» → раздел 9 интро и шаги 1.1–1.7 сессии 1: каталоги, definitions.json, rabbitmq.conf, docker-compose.yml, Dockerfile, .env/конфиги, поднятие инфраструктуры)
- **[тех] Шаг 1.2, «🔬 Под капотом: … classic vs quorum»** — «(б) quorum-очереди поддерживают приоритеты ограниченно» → проверено вживую (`rabbitmq:4-management`, тот же образ, что в лабе): `PUT /api/queues/%2f/qtest` с `{"x-queue-type":"quorum","x-max-priority":10}` возвращает `400 invalid arg 'x-max-priority' for queue ... of queue type rabbit_quorum_queue` — quorum-очереди НЕ поддерживают `x-max-priority` вообще (не «ограниченно», а полностью отклоняют аргумент как невалидный) → исправить на «quorum-очереди не поддерживают приоритетные очереди (x-max-priority)» или похожую формулировку без «ограниченно».
- — остальное в части проверено целиком, расхождений не найдено: сверил code-блоки `definitions.json` (BLOCK 10), `rabbitmq.conf` (BLOCK 11), `docker-compose.yml` (BLOCK 12), `laravel-app/Dockerfile` (BLOCK 13–14), `.env`+`config/rabbitmq.php`+`config/lab.php` (BLOCK 15–16), команды шага 1.7 (BLOCK 17) в HTML — дословно совпадают с `pre_blocks.txt`; проверил `definitions.json` на валидность JSON (`python3 json.loads`) — валиден; проверил `docker-compose.yml` через `docker compose config -q` (YAML-якоря `&app`/`<<: *app` для пяти воркеров + app) — валиден, мёрдж-ключи резолвятся корректно; `config/rabbitmq.php` и `config/lab.php` — `php -l` без ошибок; сверил имена exchange/очередей/routing key между диаграммой раздела 7 (email.queue → email.retry(direct, routing "retry.{attempt}") → email.retry.1/2/3 → email.dlx(direct) → email.queue/email.dlq) и `definitions.json` — TTL 10000/30000/300000 мс и dead-letter-routing-key "email" совпадают в обоих местах; названия email.retry.1/2/3, email.dlx, email.dlq, order.queue/email.queue/analytics.queue, orders.topic, system.broadcast/broadcast.a-b-c и routing key (order.created, order.*, retry.1/2/3, email, dlq, пустой ключ для fanout) — везде согласованы между разделом 6 (сценарий), разделом 7 (диаграмма retry), диаграммой раздела 3 (архитектура) и definitions.json; описание AMQP-семантики технически корректно: единственный статический DLX+routing key на очередь (да, это ограничение AMQP/RabbitMQ), head-of-line blocking у TTL в голове очереди (соответствует официальной документации TTL/DLX — просроченное сообщение удаляется только когда доходит до головы очереди), формирование и накопление `x-death` при каждом dead-lettering (queue/reason/count/routing-keys/time — соответствует спецификации), поведение `nack(requeue=true)` (сообщение уходит в голову очереди, а не в хвост — потенциальный livelock при постоянной ошибке) — корректно; `x-max-priority=10` у `order.queue`, что max=255 но рекомендовано 1–10, что сообщение без priority получает 0, а priority выше максимума очереди обрезается — всё соответствует официальной документации Priority Queue Support; round-robin с учётом свободного prefetch-слота для competing consumers — корректное описание fair dispatch; таблица метрик UI (Ready/Unacked/Total/Incoming-Deliver-Ack rate/Redelivered/Consumer utilisation) шага 1.7 — определения соответствуют RabbitMQ Management UI; пример «ключ order.paid → только analytics.queue, ключ с опечаткой orders.created → тихо исчезает» — корректно следует из биндингов definitions.json (order.queue/email.queue привязаны только к точному order.created, analytics.queue — к order.*, mandatory-флag не установлен, так что нет unroutable-return); ссылки на разделы/шаги в этом фрагменте (#sec-7, #sec-8, #sec-9, #step-1-2 … #step-1-8) резолвятся на реально существующие id; латиница вместо кириллицы и сырые бэктики вместо `<code>` не встречены.

### Часть 4 (конец сессии 1: confirms под капотом, шаг 3.4 «три воркера happy path», FOR UPDATE/deadlock/outbox-из-воркера; сессия 2: 4.1 генератор нагрузки, 4.2 competing consumers + prefetch, 4.3 crash test/идемпотентность, 5.1 «ломаем почту», 5.2 retry-цепочка onFailure, 5.3 читатель DLQ)

- **[тех] Шаг 4.2, «Под капотом: как выбрать prefetch...»** — «Лечится маленьким prefetch или (в RabbitMQ 3.8+) consumer priority» → Consumer Priorities — фича RabbitMQ 3.2 (введена в 2013, см. официальный блог RabbitMQ «Using Consumer Priorities with RabbitMQ», 16 дек. 2013; подтверждено веб-поиском), а не 3.8+ → исправить на «начиная с RabbitMQ 3.2» (или убрать версионную привязку вовсе).

- **[противоречие] Шаг 4.3 и Шаг 5.1** — команды `LAB_CRASH_BEFORE_ACK=true docker compose up -d --force-recreate order-worker` и `LAB_SIMULATE_SMTP_FAILURE=true docker compose up -d --force-recreate email-worker` → на практике не сработают: сервисы order-worker/email-worker в docker-compose.yml (шаг 1.4) получают переменные только через `env_file: ./laravel-app/.env`, без секции `environment:`, а сама методичка чуть раньше (шаг 1.6, «Под капотом: env, config:cache и переменные для одного контейнера») прямо объясняет, что для трюка «на лету» нужно заранее добавить в сервис `environment: { LAB_X: ${LAB_X:-false} }`, иначе `env_file` побеждает — но эта строка так и не попадает в docker-compose.yml ни на шаге 1.4, ни где-либо ещё в документе (проверено `grep -n "environment:"` по всему HTML). Проверено вживую: минимальный docker-compose с `env_file` (FOO=from_envfile) и `FOO=true docker compose up -d --force-recreate` — контейнер после пересоздания продолжает видеть `FOO=from_envfile`, а не `true`. Итог: шаги 4.3 и 5.1 в текущем виде не включат симуляцию краша/SMTP-сбоя → либо добавить в docker-compose.yml для order-worker/email-worker `environment: { LAB_CRASH_BEFORE_ACK: ${LAB_CRASH_BEFORE_ACK:-false} }` / `environment: { LAB_SIMULATE_SMTP_FAILURE: ${LAB_SIMULATE_SMTP_FAILURE:-false} }`, либо (проще и без правки compose) заменить оба места на паттерн «правим .env → docker compose up -d --force-recreate ...», уже использованный в шаге 4.2 для WORKER_PREFETCH.

- **[тех] Шаг 5.2** — «TTL retry.3 можно уменьшить в definitions.json (docker compose down -v && up — определения перечитаются)» → флаг `-v` без необходимости удаляет именованный volume `pgdata` (все products/orders/outbox_messages/processed_messages), хотя у самого RabbitMQ в docker-compose.yml (шаг 1.4) нет volume для данных вообще (только два ro bind-mount конфигов) — `load_definitions` перечитается и без `-v`, простым `docker compose up -d --force-recreate rabbitmq` (или `docker compose restart rabbitmq`, т.к. definitions.json — bind-mount, а не скопированный при создании контейнера файл) → заменить рекомендацию, не трогая postgres-volume.

Проверено дополнительно (расхождений не найдено): имена очередей/exchange/routing key/заголовков во всех блоках части 4 (order.queue, email.queue, analytics.queue, email.retry→retry.1/2/3, email.dlx→email/dlq, message_id/x-retry-count/x-last-error/x-death) сверены с pre_blocks.txt (BLOCK 8, 10, 31-33, 40, 42) — совпадают; код всех приведённых PHP-классов (OrderWorkerCommand, EmailWorkerCommand + onFailure, AnalyticsWorkerCommand, PublishOrdersCommand, FailedEmailWorkerCommand) прогнан через `php -l` — синтаксических ошибок нет; воспроизведён на PHP 8.5 баг `$headers + [...]` из «Под капотом» шага 5.2 — подтверждено, что старое значение x-retry-count побеждает и не растёт, а `array_merge($headers, [...])`/`[...] + $headers` действительно чинят это — объяснение в методичке технически верно; численно проверена сумма TTL retry-цепочки 10с+30с+300с = 5 мин 40 с (совпадает с текстом); проверена арифметика prefetch confirm-round-trip (1-2 мс → 500-1000 msg/s); сверена семантика reject/nack (reject — AMQP 0-9-1 одиночный, nack — расширение RabbitMQ с multiple) и позиция requeue (RabbitMQ docs: «to its original position... or closer to the head») — соответствует тексту; heartbeat=30 → детект обрыва ~60с (2×heartbeat) — соответствует AMQP-семантике; x-delivery-limit как отличие quorum/classic — соответствует.


### Часть 1 (введение/оглавление, раздел 1 «Разбор исходного плана», раздел 2 «Как RabbitMQ устроен внутри» 2.1–2.9, раздел 3 «Итоговая архитектура», раздел 4 «Технологический стек и структура проекта», начало раздела 5 «Транзакционная публикация событий (Outbox Pattern)» до постановки задачи outbox-relay)
- **[противоречие] Раздел 4, таблица стека, строка «БД»** — «PostgreSQL 16 → orders, outbox, processed_messages, order_events» → таблица называет таблицу аутбокса «outbox», а везде далее в этой же части (раздел 5: «INSERT INTO outbox_messages (id, routing_key, payload, status='pending')», «SELECT ... WHERE status='pending'» из блока «Решение с Outbox»; дерево проекта того же раздела 4 — модель `OutboxMessage`) и в pre_blocks.txt (BLOCK 19 — `Schema::create('outbox_messages', ...)`, BLOCK 23 — `OutboxMessage::create([...])`) реальное имя таблицы — `outbox_messages` → исправить ячейку таблицы на «orders, outbox_messages, processed_messages, order_events».
- — остальное в части проверено целиком, расхождений не найдено: сверил ASCII-диаграммы «Жизнь одного сообщения» (2.8) и «Итоговая архитектура» (раздел 3) с pre_blocks.txt BLOCK 1–6 — совпадают дословно; сверил таблицу 4 типов exchange (2.2) с реальными типами в definitions.json (BLOCK 10): orders.topic=topic, email.retry/email.dlx=direct, system.broadcast=fanout, headers не используется — совпадает; проверил семантику topic-wildcard из текста («order.* не матчит order.item.added», «order.# матчит и order.created, и голый order») — соответствует официальному AMQP topic-matching (RabbitMQ Tutorial 5, где `#` матчит ноль-или-более сегментов, в т.ч. пример `lazy.#` матчит и `lazy`); поднял живьём контейнер `rabbitmq:4-management` (тот же тег, что в лабе) — версия оказалась 4.3.5, что подтверждает актуальность фразы «глобальный prefetch (global=true) в RabbitMQ 4 удалён» (по данным офиц. release-notes/Amazon MQ, начиная с 4.3 basic.qos(global=true) закрыт по умолчанию); проверил через веб-поиск заявление раздела 1 про `vladimir-yuldashev/laravel-queue-rabbitmq` v15 — версия 15.0.0 (2026) действительно требует `illuminate/queue ^10|^11|^12|^13`, то есть поддержка Laravel 13 подтверждается; сверил дерево каталогов проекта (раздел 4) с namespace/путями PHP-классов, упомянутых в первых пяти разделах (OutboxWriter, OrderController, AmqpConnectionFactory, AbstractAmqpWorker, *WorkerCommand, Jobs/SendOrderEmailJob, Lab/PublishOrdersCommand, Lab/BroadcastCommand) — совпадают; сверил таблицу очередей раздела 3 (order.queue↔order.created, email.queue↔order.created, analytics.queue↔order.*) с bindings в definitions.json — совпадает; сверил диапазон priority (0–10 в тексте раздела 1/API) с `x-max-priority=10` из диаграммы раздела 3 — согласовано; проверил на латиницу вместо кириллицы и «сырые» бэктики вместо `<code>` в HTML этой части (строки ~192–510) — не найдено; сноски 1–17 (разделы 1–2.3) идут строго по возрастанию в порядке первого упоминания в тексте (проверено по `id="ref-N"` в исходнике) — расхождение нумерации, которое начинается дальше на терминах 18/20/21, уже зафиксировано агентом части 5 (раздел 11), повторно не заводил во избежание дубля.

### ✍️ Решение по вычитке
- [ ] ⭐ A — исправить всё из вычитки выше
- [ ] B — исправить только [тех] и [противоречие]
- [ ] C — выборочно (отметь пункты выше)
