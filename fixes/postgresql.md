# PostgreSQL Lab (Coffee Shop изнутри) — находки

Методичка: `postgresql/PostgreSQL_Lab_CoffeeShop.html` (добавлена 25.09). Битых якорей нет (432 внутренние ссылки, все «шаг X.Y» существуют).
README, карточка и методичка согласованы: PostgreSQL 17 в Docker + psql + pgbench, без ORM; 7 сессий (0–6).
Имя папки проекта в методичке приведено к имени репозитория (`postgresql-lab`) 25.09.

---

## 📖 Вычитка методички

### Часть 1 (раздел 1)
- — раздел 1 «Разбор задачи» проверен целиком, расхождений не найдено: сверил TOC vs заголовок раздела, все ссылки «шаги N.N–N.N» в 8 блоках 💡/РЕШЕНИЕ (3.1–3.4, 4.1–4.4, 7.2, 7.3–8.1, 9.1–10.2, 11.1–11.4, 6.1, 0.1–0.5, 2.2, 12.3) против оглавления сессий 0–6 — все указывают на реально существующие и тематически подходящие шаги; числа предметной области (20 кофеен, 100 000 клиентов, 200 позиций меню, 1 000 000 заказов, 2,5 млн позиций, 3 млн событий) совпадают с диаграммой схемы в разделе 3; нумерация сносок глоссария 1–5 в этом разделе последовательна без пропусков/дублей.

### Часть 2 (разделы 2–8.1)
- **[противоречие] Раздел 3, диаграмма схемы данных** — «shops (20) ◄… inventory (8 000) PK (shop_id, product_id) … products (200)» → при PK по паре (shop_id, product_id) и всего 20 магазинах × 200 товаров максимум возможных строк в `inventory` — 4 000 (20×200), а в диаграмме заявлено 8 000 строк, что превышает максимум и противоречит собственному PK-ограничению, указанному в той же диаграмме → исправить число строк inventory на ≤4000 (например, если не в каждом магазине есть все 200 товаров) либо убрать/скорректировать PK.
- **[текст] Раздел 2.1, диаграмма процессов** — «background writer — понемногу пишет грязные страницы заранее, чтобы backend'ам не пришлось» → предложение обрывается на середине, не хватает продолжения (что именно не пришлось бы делать backend'ам — например, «...не пришлось ждать записи при вытеснении страницы») → дописать концовку фразы.
- — остальное в разделах 2–8.1 проверено, расхождений не найдено: сверил актуальность технических утверждений о PostgreSQL — константы стоимости планировщика (seq_page_cost=1.0, random_page_cost=4.0, cpu_tuple_cost=0.01, cpu_index_tuple_cost=0.005, cpu_operator_cost=0.0025) и deadlock_timeout=1с проверены живым запросом к `pg_settings` в контейнере postgres:13.3-alpine — значения совпадают; `pg_backend_pid()` проверен вызовом — существует, возвращает integer; таблицы конфликтов блокировок строк (FOR UPDATE/FOR NO KEY UPDATE/FOR SHARE/FOR KEY SHARE) и таблиц (ACCESS SHARE/ROW EXCLUSIVE/SHARE UPDATE EXCLUSIVE/SHARE/ACCESS EXCLUSIVE) сверены с документацией PostgreSQL — точно соответствуют; таблица уровней изоляции (аномалии × Read Committed/Repeatable Read/Serializable) соответствует реальному поведению Postgres (RR ловит потерянное обновление через 40001, но не write skew; Serializable ловит всё через SSI); код ошибки 40001/40P01 верны; EXPLAIN (BUFFERS по умолчанию с PG18) — верно; арифметика примеров (11 255 страниц × 8КБ ≈ 88 МБ; customer_id=42 при 93 000 клиентов → ≈11 строк; status=completed 95% → 950 000 строк) — проверена, сходится; все ссылки «шаг N.N»/«раздел N» (2.5, 2.6, 3.3, 3.4, 4.4, 5.1, 5.3, 5.4, 6, 8.2, 8, 9, 10.1, sec-2, sec-5) в разделах 2–7 и Laravel-таблице (раздел 2.7) сопоставлены с оглавлением сессий — все указывают на подходящие по теме шаги; названия таблиц/колонок (orders, order_items, customers, order_events, inventory, shifts, reviews, customer_id, created_at, status, total, email, stock, bonus_points) используются согласованно во всех разделах; сноски глоссария 6–40 последовательны без пропусков/дублей; латиницы вместо кириллицы и сырых бэктиков не найдено (grep по HTML-диапазону строк 1–770).

### Часть 12 (раздел 9, шаги 11.4–12.3)
- — вся часть (хвост шага 11.3 «Долгая транзакция и горизонт», шаги 11.4 «Autovacuum, HOT, fillfactor, wraparound», 12.1 «Партиционирование по месяцам», 12.2 «Жизнь с партициями», 12.3 «Production Hell») проверена целиком, расхождений не найдено. Сверено: (1) все имена таблиц/колонок против схемы шага 1.3 и сида шага 2.1 — `order_events(id, order_id, type, payload, created_at)` без FK (совпадает с «журнал событий» на диаграмме раздела 3), `orders`/`order_items`/`inventory` PK `(shop_id, product_id)` (используется в задании 5 «Production Hell» для `ON CONFLICT (shop_id, product_id)»), `customers.bonus_points`/`email` (не индексирована/индексирована — сверено с реальными индексами `customers_email_lower_uniq` и `customers_email_trgm` из более ранних шагов); (2) числа сида — `order_events ≈ 3 000 000` (реальный вывод `n_live_tup=2998481` из более раннего шага) и объём `orders`/`order_items` — совпадают с расчётами в тренировках 11.4 (50+0,2×100000=20050 для customers, 50+0,2×2500000=500050 для order_items); (3) технические утверждения воспроизведены вживую в контейнере postgres:13.3-alpine с теми же выражениями дат, что и в сиде (`now() - (1000000-g)*interval '63 seconds'` для order_events), при этом текущая дата контейнера/хоста (2026-09-25) совпадает с расчётной «сегодня» методички — получено ровно 28 партиций (27 месячных + default) и `Subplans Removed: 24` для `created_at >= now() - interval '3 days'` — точное совпадение с текстом; HOT-счётчики `100000|0` до `fillfactor=70`+VACUUM FULL и `100000|43425` после — воспроизведены точь-в-точь; текст ошибок `unique constraint on partitioned table must include all partitioning columns`, `updated partition constraint for default partition "order_events_default" would be violated by some row`, `no partition of relation "order_events_p" found for row` — воспроизведены дословно; `DETACH PARTITION ... CONCURRENTLY` в PG13 закономерно не поддерживается (синтаксис появился в PG14) — это не ошибка методички, целевая версия лабы PG17; (4) все ссылки «шаг N.N»/«раздел N.N» (11.1, 4.4, 1.1, 8.4, 10.1, sec-12, sec-13) ведут на реально существующие и тематически подходящие якоря; (5) все 10 заданий Production Hell решаемы техникой, изученной в предыдущих сессиях лабы (статистика/ANALYZE — 4.1, FOR UPDATE — 9.1, миграции без простоя — 10.1, SKIP LOCKED — 9.2, ON CONFLICT/дедлоки — 9.3, \gexec/pg_stat_statements — 6.1, типы параметров — 4.4, партиционирование — 12.1–12.2, Serializable/write skew — 8.1), внутренних противоречий не найдено; (6) латиницы вместо кириллицы и сырых бэктиков нет (grep по HTML-диапазону строк 4114–4368, единственное смешение алфавитов — намеренное «антиwraparound»).

### Часть 11 (шаги 9.3 (хвост тренировки) – 11.3)
- **[тех] Шаг 9.3, 🏋️ Тренировка, Задания 1–3** — «`BEGIN; UPDATE customers SET bonus_points = bonus_points - 5 WHERE id = 1;` … `BEGIN; UPDATE customers SET bonus_points = bonus_points - 5 WHERE id = 2;`» (перевод бонусов для демонстрации дедлока, затем «лечение» через `ORDER BY … FOR UPDATE`, затем повтор с `deadlock_timeout='5s'`) → у customers.bonus_points `DEFAULT 0` (схема 1.3) и сид (2.1) эту колонку вообще не заполняет, то есть у всех клиентов, включая id=1 и id=2, bonus_points = 0; на той же колонке есть `CHECK (bonus_points >= 0)`. Самый первый `UPDATE … SET bonus_points = bonus_points - 5 WHERE id = 1` падает сразу: `ERROR: new row for relation "customers" violates check constraint "customers_bonus_points_check"`. Проверено в контейнере postgres:13.3-alpine на таблице с такой же колонкой/CHECK — воспроизводится один в один. Ни дедлок, ни «лечение», ни повтор с deadlock_timeout выполнить как написано нельзя. В соседних шагах (7.x, судя по pre_blocks.txt) автор сам избегает этой ловушки, всегда сперва делая `bonus_points + N`, прежде чем вычитать — здесь это правило нарушено. → Добавить перед переводом начисление баллов, например `UPDATE customers SET bonus_points = bonus_points + 100 WHERE id IN (1,2);`, либо переписать демо на другую колонку без CHECK (например, inventory.stock, как в основном сценарии этого же шага).
- **[противоречие] Шаг 10.1** — «`-- попробуйте на orders: индекс для списка заказов кофейни (из шага 6.1) без блокировки записи`  `CREATE INDEX CONCURRENTLY orders_shop_created_idx ON orders (shop_id, created_at);`» → индекс с точно таким именем и определением уже создан в шаге 3.4 (там же — «Задание 1») и нигде до 10.1 не удаляется (по pre_blocks.txt — единственные упоминания `orders_shop_created_idx`: создание в 3.4 и это же создание в 10.1, DROP INDEX для него нет вообще). Сам шаг 3.4 в тексте даже обещает: «Этот же индекс предлагается в шагах 6.1 и 10.1; в 10.1 перед CREATE INDEX CONCURRENTLY удалите его DROP INDEX» — но в самом шаге 10.1 команды `DROP INDEX orders_shop_created_idx;` нет. При последовательном прохождении лабы (как предполагается) `CREATE INDEX CONCURRENTLY` упадёт с `ERROR: relation "orders_shop_created_idx" already exists`. Плюс ссылка «(из шага 6.1)» ошибочна: в шаге 6.1 (N+1, sql/20_n_plus_one.sql) этот индекс вообще не упоминается, он впервые появляется в 3.4. → Перед `CREATE INDEX CONCURRENTLY` добавить `DROP INDEX orders_shop_created_idx;` (как и обещано в 3.4) и поправить ссылку на «из шага 3.4».
- — остальное в шагах 9.4–11.3 проверено, расхождений не найдено: имена таблиц/колонок (customers, inventory, orders, order_events, shops) сверены со схемой 1.3 и сидом 2.1; pg_stat_activity/pg_locks/pg_blocking_pids — состав колонок, состояния (active/idle/idle in transaction/idle in transaction (aborted)), wait_event_type='Lock'/wait_event='transactionid' для ожидания строковой блокировки, поведение pg_cancel_backend (SIGINT, только текущий оператор) и pg_terminate_backend (SIGTERM, FATAL у жертвы) — корректны; очередь блокировок ALTER TABLE (ACCESS SHARE → ACCESS EXCLUSIVE → ACCESS SHARE в очереди за ALTER) и lock_timeout — корректны; таблица «Рецепты безопасных миграций» (ADD COLUMN DEFAULT константа/now() не volatile → мгновенно с PG11, clock_timestamp()/gen_random_uuid() volatile → перезапись; NOT NULL через NOT VALID CHECK → VALIDATE → SET NOT NULL; FK через NOT VALID → VALIDATE) соответствует поведению PG 11+/17; ошибка `CREATE INDEX CONCURRENTLY … cannot run inside a transaction block` — корректна; advisory locks (pg_try_advisory_lock/pg_advisory_unlock/pg_advisory_xact_lock, реентерабельность сессионных блокировок, утечка при PgBouncer transaction pooling, hashtext() на bigint-перегрузке) — корректны; MVCC-демо на cups (xmin/xmax/ctid/heap_page_items, цепочка версий при UPDATE, REPEATABLE READ видит старую версию, hint bits/CLOG, FOR UPDATE пишет t_xmax) — числа внутри самой демонстрации (858/859/860, lp 1–4) взаимно согласованы; VACUUM/VACUUM FULL на mvcc_demo (autovacuum_enabled=false, освобождение места «внутри файла» без уменьшения файла, VACUUM FULL — ACCESS EXCLUSIVE и сжатие, pgstattuple/REINDEX INDEX CONCURRENTLY (PG12+)/pg_repack) — корректны; горизонт VACUUM в 11.3 (backend_xmin, «dead but not yet removable», REPEATABLE READ блокирует уборку) — корректно. Проверено сверкой с pre_blocks.txt (шаги 1.3, 2.1, 3.4, 6.1) и тестовым запуском CHECK-констрейнта на bonus_points в контейнере postgres:13.3-alpine.

### Часть 3 (конец раздела 8: 8.3–8.8; начало раздела 9; Сессия 0, шаги 0.1–0.4 (начало))
- — часть проверена целиком, расхождений не найдено. Сверено: (1) теория 8.3–8.8 (снимок/видимость, VACUUM, autovacuum и горизонт, freeze/wraparound XID, HOT-обновления, партиционирование, N+1) — арифметика пересчитана вручную: `50 + 0,2 × 1 000 000 = 200 050` мёртвых версий для autovacuum_vacuum_threshold/scale_factor по умолчанию — сходится; тайминги N+1 (`1 + 100×(0,5+0,05+0,005) ≈ 56 мс` и eager `≈1,7 мс`, 20 мс round-trip → 2 c) — сходятся; утверждения про PK/UNIQUE партиционированной таблицы, обязанные включать ключ партиционирования, про AGE(datfrozenxid) и HOT — соответствуют реальному поведению PostgreSQL. (2) Песочница шага 0.1 (`sandbox.clients/drinks/orders/bonus_cards`) поднята и все запросы шагов 0.1–0.3 и начала 0.4 прогнаны вживую в контейнере `postgres:13.3-alpine` — результаты (NULL-семантика, `count(city)=4`, `city <> 'Amsterdam'` → Иван/Ева, CROSS JOIN 35 строк, INNER/LEFT/RIGHT/FULL JOIN, ловушка условия в WHERE у LEFT JOIN, self-join `referred_by`) совпадают с приведёнными в тексте таблицами вывода один в один. (3) Ссылки «шаг 0.3» (из 0.1), «шаг 5.1» (из 0.2, Nested Loop/Hash Join/Merge Join) сверены с оглавлением сессий — обе ведут на реально существующие и тематически подходящие шаги. (4) Раскладка терминов (PK/FK/один-ко-многим/многие-ко-многим/ссылка на себя) и имена таблиц/колонок согласованы между шагами. Латиницы вместо кириллицы и сырых бэктиков не найдено.

### Часть 4 (Сессия 0, шаг 0.4 (продолжение)–0.5; Сессия 1, шаги 1.1–1.3 (до Задания 3, обрыв на границе частей))
- **[противоречие] Шаг 0.4, после `EXISTS/NOT EXISTS читается как сам вопрос и не плодит дубли.`** — «Здесь же знакомая по шагу <a href="#step-2-2">2.2</a> ловушка: `NOT IN (SELECT client_id FROM orders)` вернёт пусто...» → шаг 2.2 («SQL за пределами CRUD») идёт в Сессии 1, значительно позже шага 0.4 из Сессии 0 (сама методичка это подтверждает: сразу после тренировки 0.5 написано «Если шаги 0.1–0.5 и их тренировки получились — вы готовы к шагу 2.2»), то есть на шаге 0.4 читатель физически не мог видеть шаг 2.2 и не может быть с ним «знаком»; сама эта ловушка (NOT IN + NULL) впервые и полностью разбирается прямо здесь, в Задании 4 тренировки этого же шага 0.4 → убрать слово «знакомая» и ссылку назад или заменить на форвард-формулировку («эта же ловушка ещё раз встретится в шаге 2.2»).
- **[противоречие] Шаг 1.2, 🏋️ Тренировка, Задание 2** — «`SELECT count(*) AS n FROM customers \gset`» → таблица `customers` создаётся только в следующем шаге 1.3 (`sql/01_schema.sql`, `CREATE TABLE customers (...)`); при последовательном прохождении лабы (1.1 → 1.2 → 1.3, как предписывают «ПРАВИЛА ИГРЫ») на шаге 1.2 этой таблицы ещё не существует, и запрос упадёт с `ERROR: relation "customers" does not exist` — проверено логически по последовательности шагов и подтверждено пустой базой в контейнере `postgres:13.3-alpine` до выполнения 1.3 → заменить на таблицу, уже существующую на этом этапе (например, `sandbox.clients` из Сессии 0), либо перенести задание после шага 1.3.
- — остальное в шагах 0.4 (продолжение)–1.3 проверено, расхождений не найдено: все запросы 0.4 (CROSS JOIN 2×5=10 строк, anti-/semi-join → Олег/Ева и Анна/Иван/Мария, тренировка 1–4) и 0.5 (JOIN+GROUP BY, fan-out на `bonus_cards`, тренировка 1–4) прогнаны вживую в `postgres:13.3-alpine` на той же песочнице — числа (Анна cups=6/points=300 до исправления, 3/150 после; Мария 4/140 → 4/70; выручка по напиткам 14.00/9.60/4.20/4.00/0; траты клиентов 9.10/2, 7.50/0, 13.10/1; города Amsterdam 2/4, Almere 1/0, Utrecht 1/2, неизвестно 1/0) совпадают один в один. Схема кофейни шага 1.3 (`sql/01_schema.sql`) создана вживую в `postgres:13.3-alpine` без ошибок; `\d orders` показывает в Indexes только `orders_pkey`, как и заявлено; тренировка 1.3 проверена live — `INSERT ... status='lost'` даёт `ERROR: ... violates check constraint "orders_status_check"`, `INSERT ... customer_id=999999999` даёт `ERROR: ... violates foreign key constraint "orders_customer_id_fkey"` (имена ограничений сгенерированы Postgres автоматически и совпадают дословно), `SQLSTATE 23514/23503` верны; `BEGIN; ALTER TABLE products ADD COLUMN is_seasonal ...; ROLLBACK;` — колонка действительно исчезает после отката (DDL транзакционен), проверено вживую. docker-compose.yml (образ `postgres:17`, `shared_preload_libraries`, `shm_size: 1g`, `work_mem`/`shared_buffers` и т.д.) и таблица метакоманд psql (`\dt+`, `\di+`, `\d`, `\dx`, `\x`, `\timing`, `\e`, `\i`, `\watch`, `\gset`/`\gexec`, `\h`) — синтаксически и по смыслу верны. Ссылки «шаг 8.2» (CHECK как последняя линия обороны), «шаги 6.1, 6.3» (pg_stat_statements), «шаг 5.1» (work_mem) сверены с оглавлением сессий — ведут на подходящие по теме шаги. Латиницы вместо кириллицы и сырых бэктиков не найдено.

### Часть 5 (разделы 1.3 (хвост тренировки) – 3.2)
- — часть проверена целиком (хвост тренировки шага 1.3, шаг 2.1 «Миллион заказов за минуту», шаг 2.2 «SQL за пределами CRUD», шаг 2.3 «Как таблица лежит на диске», шаг 3.1 «Первый EXPLAIN» и начало тренировки к 3.2), расхождений не найдено. Схему и сид из шагов 1.3/2.1 поднял в контейнере postgres:13.3-alpine (таблицы shops/customers/products/orders/order_items/inventory/shifts/order_events/reviews, полный sql/02_seed.sql с тем же `setseed(0.42)`) и сверил вживую: итоговые размеры таблиц после `\i /sql/02_seed.sql` (order_events 377 MB/2 998 481 строк, order_items 197 MB/2 500 000, orders 109 MB/1 000 000, reviews 20 MB, customers 20 MB) совпали с «Ожидаемым результатом» в шаге 2.1 день в день; распределение статусов заказов (задание 1 шага 2.1: completed ~947 000, cancelled ~50 000, остальные по ~750) подтвердилось (947360/49640/771/749/741/739); доля заказов у первых 100 клиентов (задание 2, «≈3 %») и объяснение сдвига `random()^2` (задание 4) математически верны; `relpages`/`reltuples`/формула стоимости Seq Scan из раздела 2.6 (1466×1.0 + 100000×0.01 + 100000×0.0025 = 2716) воспроизведена почти точь-в-точь (1467/2717 из-за версии/RNG — не ошибка методички); шаг 2.3 — `relpages=11256`, `mb≈87`, `rows_per_page≈88`, `ctid`-примеры, Tid Scan по `ctid='(100,1)'` — всё совпало; шаг 3.1 — структура плана (Parallel Seq Scan → Sort → Gather Merge → Limit) воспроизведена; ссылки «раздел 6» (Как читать EXPLAIN) и «раздел 2.6» (модель стоимости) сверены с оглавлением/частью 1 — существуют и тематически подходят; имена колонок/таблиц (orders.version, orders.customer_id, customers.city) согласованы со схемой 1.3. Латиницы вместо кириллицы и сырых бэктиков в этом диапазоне HTML (строки ~1466–1992) не найдено.

### Часть 6 (разделы 3.2 (хвост) – 4.2)
- **[противоречие] Шаг 4.2** — «Экран бариста: активные заказы кофейни `EXPLAIN (ANALYZE, BUFFERS) SELECT id, customer_id, status, created_at FROM orders WHERE shop_id = 3 AND status IN ('paid', 'preparing') ORDER BY created_at; … Активных заказов в системе около 3 000 из миллиона (0,3 %)»` → запрос фильтрует только 2 статуса из 4 «активных» (`paid`, `preparing`), а по собственной статистике сида из шага 2.1 («остальные — по ~750») это даёт ≈1500 заказов, а не 3000. Проверено вживую на сиде из шага 2.1 (postgres:13.3-alpine, тот же `setseed(0.42)`): `status IN ('paid','preparing')` → 1512 строк (0,15 %), и только если добавить `'new'` и `'ready'` — ровно 3000 (0,3 %), то есть заявленное число 0,3% явно посчитано для всех 4 статусов, а не для двух, использованных в запросе. → либо расширить `status IN (...)` до всех активных статусов `('new','paid','preparing','ready')`, либо поправить текст на «≈1500 заказов (0,15 %)».
- **[противоречие] Шаг 3.4, Задание 1** — «Этот же индекс предлагается в шагах 6.1 и 10.1; в 10.1 перед CREATE INDEX CONCURRENTLY удалите его DROP INDEX.» → в самом шаге 10.1 (см. находку по части 11) команды `DROP INDEX orders_shop_created_idx;` перед `CREATE INDEX CONCURRENTLY orders_shop_created_idx ON orders (shop_id, created_at);` нет, из-за чего при последовательном прохождении лабы команда упадёт с `ERROR: relation "orders_shop_created_idx" already exists` (индекс с этим именем и определением уже создан здесь, в задании 1 шага 3.4). Также в шаге 6.1 этот индекс лишь описывается словами («создайте индекс и сравните»), явного `CREATE INDEX orders_shop_created_idx` там нет — само упоминание «в шагах 6.1 и 10.1» корректно в смысле «идея обсуждается», но обещание про DROP INDEX в 10.1 не выполнено. → добавить `DROP INDEX orders_shop_created_idx;` в шаг 10.1 перед `CREATE INDEX CONCURRENTLY` (как и обещано здесь).
- — остальное в шаге 3.2 (хвост тренировки), 3.3 «Первый B-tree», 3.4 «Составной индекс» и 4.1 «Статистика» проверено, расхождений не найдено: воспроизвёл на сиде из шага 2.1 (postgres:13.3-alpine) — `EXPLAIN (ANALYZE, TIMING OFF)` действительно убирает `actual time=` у узлов, но сохраняет `rows`/`loops` и итоговый `Execution Time` (задание 3 к 3.2); индекс `orders_customer_id_idx` даёт план Bitmap Index Scan → Bitmap Heap Scan → Sort → Limit, как в шаге 3.3; запрос «FK без индекса» из 🔬-врезки 3.3 корректно находит колонки без индекса на левом префиксе; DELETE с триггерами `Trigger for constraint order_items_order_id_fkey`/`reviews_order_id_fkey` воспроизведён дословно (задание 2 к 3.3); правило левого префикса в 3.4 (Index Scan Backward для ORDER BY по возрастанию, оба условия в Index Cond для равенства+диапазона, Sort для `customer_id IN (1,2)`, Seq Scan для условия не по левой колонке) подтверждено живыми `EXPLAIN`; составной индекс `orders_customer_created_idx` и его влияние на оценку/план для клиента 200 (rows=16 до фикса статистики) совпадает с шагом 4.1; `SET STATISTICS 1000` + `ANALYZE` действительно поднимает `n_distinct` (63296→~93 500 в тексте, у меня 61702→93475 — то же порядок величины) и добавляет клиента в MCV; формула отбора `default_statistics_target=100` → выборка 30 000 строк подтверждена (`SHOW default_statistics_target` = 100); ссылка «раздел 5.3» («телефонная книга») сверена с частью 2 — существует, тематически подходит. Латиницы вместо кириллицы и сырых бэктиков в этом диапазоне HTML (строки ~1992–2456) не найдено.

### Сверка находок (Claude, 25.09)
Каждая находка выше перепроверена по исходнику методички:
- Часть 2, `inventory (8 000)` — подтверждено: сид делает `shops CROSS JOIN products` = 20 × 200 = **4 000**.
- Часть 2, оборванная фраза про background writer — подтверждено (строка 355 HTML).
- Часть 11, дедлок на бонусах (шаг 9.3) — подтверждено, **с уточнением**: падает не первое списание у клиента 1 (у него после шага 7.1 есть 10–20 бонусов), а `[B] … - 5 WHERE id = 2` — у клиента 2 бонусов всегда 0 (все прошлые списания с него падают на CHECK). Исправление: перед заданием `UPDATE customers SET bonus_points = bonus_points + 100 WHERE id IN (1, 2);`.
- Часть 11 и Часть 6, `orders_shop_created_idx` — **одна и та же находка** с двух сторон (3.4 обещает `DROP INDEX` в 10.1, в 10.1 его нет; ссылка «шаг 6.1» неверна в обоих местах). Считать один раз.
- Часть 4, «знакомая по шагу 2.2 ловушка» в 0.4 и `customers` в шаге 1.2 (таблица создаётся в 1.3) — подтверждено.
- Часть 6, «активных заказов около 3 000 (0,3 %)» для `status IN ('paid','preparing')` — подтверждено: по сиду по ~750 на статус → ~1 500 (0,15 %); 3 000 — это все четыре активных статуса (как в предикате индекса строкой ниже).

**Итого по проверенным частям: 7 уникальных находок** — [тех] 1, [противоречие] 5, [текст] 1.

### ⏸ Не вычитано (вычитка остановлена по просьбе пользователя)
- Часть 7–8 (шаги 4.2 хвост – 6.x: статистика, покрывающие индексы, JOIN, GIN, BRIN, N+1, пагинация) — агент остановлен, записей нет.
- Часть 9–10 (сессии 4–5: транзакции, изоляция, блокировки, начало дедлоков) — агент остановлен, записей нет.
- Часть 13 (разделы 10–13: чек-лист, глоссарий, вопросы) — агент остановлен, записей нет (успел сверить часть ссылок чек-листа — расхождений не видел, но запись не сделана).

Продолжить: `python3 fixes/_tools/prep.py postgresql <scratch>/proof-postgresql`, запустить агентов на части 7–8, 9–10, 13 по шаблону из `_proofread.md`.

### Часть 7 (хвост шага 4.2, шаги 4.3–4.4, начало сессии 3: шаг 5.1)
- **[тех] Шаг 4.2** — «`CREATE INDEX orders_active_idx ON orders (shop_id, created_at) WHERE status IN ('new', 'paid', 'preparing', 'ready'); … SELECT pg_size_pretty(pg_relation_size('orders_active_idx')) AS partial, pg_size_pretty(pg_relation_size('orders_customer_created_idx')) AS full_index;`» → мысль шага — «частичный индекс весит намного меньше полного индекса по тем же колонкам» («112 KB вместо десятков мегабайт»), но колонкой сравнения взят `orders_customer_created_idx` — индекс по совершенно другим колонкам (`customer_id, created_at DESC`, создан в шаге 3.4/используется в 4.1, 4.3), а не полный (без WHERE) аналог `orders_active_idx` по тем же `(shop_id, created_at)`, которым уже является `orders_shop_created_idx` (создан в шаге 3.4, Задание 1, и ещё существует на этот момент лабы). Сравнение размеров разнородных индексов не демонстрирует заявленную мысль про частичный vs полный индекс → заменить в обоих `SELECT`'ах `orders_customer_created_idx` на `orders_shop_created_idx`.
- — остальное в хвосте шага 4.2 (частичный уникальный индекс по `lower(email)`, тренировка 1–4), шаге 4.3 (покрывающий индекс, `INCLUDE`, Index Only Scan / Heap Fetches и связь с MVCC, тренировка 1–3), шаге 4.4 (антипаттерны для индекса `orders_created_at_idx`, `orders_status_idx`, корреляция, `text_pattern_ops`, тренировка 1–4) и шаге 5.1 (Nested Loop/Hash Join/Merge Join, `random_page_cost`, `work_mem`, тренировка 1–3) проверено, расхождений не найдено: имена индексов согласованы со схемой шага 3.4 (`orders_customer_created_idx`, `order_items_order_id_idx`) и друг с другом (`orders_active_idx` пересоздаётся в 4.3-Задании 1 с тем же именем и предикатом + `INCLUDE`); арифметика `actual … rows=N loops=M` в планах 5.1 непротиворечива (833333×3≈2,5 млн строк `order_items`, 12090×3≈36000 заказов за 30 дней, 18134×2≈36269 — совпадает с `loops=36269` внутреннего `Index Scan` чуть ниже; `Memory: 6286kB` в примере `work_mem='16MB'` совпадает с ответом тренировки 3 «около 6,3 МБ»); латиницы вместо кириллицы и сырых бэктиков в диапазоне HTML (шаги 4.2–5.1) не найдено (проверено скриптом на смешанные кириллица/латиница слова и grep по обратным кавычкам).

### Часть 8 (шаги 5.2 GIN, 5.3 BRIN, 5.4 расширенная статистика, 6.1 N+1, 6.2 пагинация, начало 6.3)
- — раздел проверен целиком, расхождений не найдено: `CREATE EXTENSION pg_trgm` уже выполнен заранее (HTML, строка 1468, до первого использования `gin_trgm_ops` в 5.2); вживую в postgres:17 (docker) воспроизведены `to_tsvector('english', 'Cold latte, never again')` → `'cold':1 'latt':2 'never':3`, `websearch_to_tsquery('english', 'cold latte -service')` → `'cold' & 'latt' & !'servic'` и `show_trgm('vries')` — в исходном HTML (строка 2707) корректно `{"  v"," vr","es ",ies,rie,vri}` (с двумя пробелами в первом элементе), что совпало с живым выводом; расхождение с одним пробелом видно только в текстовом слепке `part_08.txt` — это артефакт схлопывания пробелов при экстракции текста, а не ошибка методички (в самой HTML/`<pre><code>` пробелы на месте) — не репортю как находку. Пример из шага 5.4 (`deliveries`, `CREATE STATISTICS … (dependencies)`) воспроизведён вживую: до статистики `rows≈495` (текст: 503) при факте 5000, после — `rows≈4977` (текст: 4983) — тот же порядок величин, направление ошибки и её исправление совпадают с текстом; отдельно воспроизведена тренировка 1 к 5.4 (`ndistinct`, `GROUP BY street_id, district_id`) — получил ровно `rows=2000` → `rows=200`, дословно как в ответе. BRIN/B-tree размеры проверены на непротиворечивость (64 MB / 32 kB ≈ 2048×, текст утверждает «в 2000 раз меньше» — совпадает). Индексы `orders_created_id_idx` (6.2), `order_events_payload_gin`, `order_events_channel_idx`, `reviews_body_fts` и функция `pgbench`/`\gexec`-приём в 6.1 согласованы со схемой (`order_items.order_id/product_id/qty`, `order_events.payload`, `reviews.body`) и с индексами, созданными в предыдущих шагах. Латиницы вместо кириллицы и сырых бэктиков не найдено.

### Часть 9 (хвост шага 6.3: мини-проект и подготовленные запросы; сессия 4: шаги 7.1–7.4, частично)
- — раздел проверен целиком, расхождений не найдено: ссылка мини-проекта «А. … индекс по `created_at` не используется (шаг 4.4)» указывает на реально существующий и тематически подходящий шаг; SQLSTATE-коды `40001` (serialization_failure) и `40P01` (deadlock_detected) и параметр `plan_cache_mode = force_custom_plan` — корректные официальные названия; опция `pgbench --max-tries` подтверждена вживую (`pgbench --help` в контейнере postgres:17 показывает `--max-tries=NUM`, дефолт 1). Шаг 7.1 воспроизведён вживую от начала до конца на минимальной таблице `customers(id, bonus_points int check (bonus_points>=0))`: `pg_current_xact_id_if_assigned()` пуст на чтении и после `SELECT`, получает число после первого `UPDATE`; нарушение `CHECK` внутри транзакции даёт `ERROR: current transaction is aborted, commands ignored until end of transaction block`, а `COMMIT` в этом состоянии интерпретируется как `ROLLBACK` — дословно как в тексте. Шаг 7.4 (`ERROR: could not serialize access due to concurrent update` при повторном `UPDATE` той же строки на `REPEATABLE READ` после конкурентного коммита) воспроизведён вживую на минимальной таблице `inventory(shop_id, product_id, stock check(stock>=0), PK(shop_id, product_id))` — точное совпадение сообщения об ошибке. Шаги 7.2–7.3 (потерянное обновление, атомарный `UPDATE`, `FOR UPDATE`, оптимистичная блокировка через `orders.version`, неповторяемое чтение/фантомы в Read Committed) используют колонки `inventory.stock`, `orders.version`, `orders.status`, существующие по схеме и упомянутые в более ранних частях — конкретные числа в примерах (44/50, 9/50 и т.д.) — иллюстративные и помечены в тексте как «у вас может быть другое», не проверялись вживую под нагрузкой `pgbench` (не влияет на корректность методики). Латиницы вместо кириллицы и сырых бэктиков не найдено.

### Часть 10 (шаги 8.1–9.3: Serializable/write skew, CHECK/UNIQUE/EXCLUDE/ON CONFLICT, строковые блокировки, SKIP LOCKED, начало дедлока)
- **[тех] Шаг 9.2 «Очередь заказов … на SKIP LOCKED»** — «Теперь уберите `SKIP LOCKED` и повторите: [B] будет ждать [A], а после `COMMIT` [A] получит… `UPDATE 0` — строка уже не `paid`, а следующую он не искал.» → перепутан субъект: после того как [A] делает `COMMIT`, именно [B] (тот, кто ждал блокировку) перечитывает `WHERE status = 'paid'` по свежему снимку Read Committed, видит, что строку уже забрал [A], и получает `UPDATE 0` — а не сам [A], который уже успешно получил `UPDATE 1` до того, как [B] вообще начал ждать → заменить «[A] получит» на «[B] получит».
- — остальное в части 10 проверено, расхождений не найдено: имена auto-constraint для inline `CHECK`/`EXCLUDE` (`inventory_stock_check`, `barista_shifts_barista_during_excl`) и текст ошибки нарушения exclusion-ограничения воспроизведены в живом Postgres 17 и совпадают дословно; семантика полуоткрытого интервала `[08:00,14:00)` в примере с `barista_shifts` подтверждена (третий INSERT с началом в 14:00 проходит); строки `pgrowlocks('orders'/'parent')` для FK-проверки при INSERT (`{"For Key Share"}`), простого `UPDATE` (`{"No Key Update"}`) и простого `DELETE` (`{Update}`) воспроизведены в живом Postgres и совпадают с текстом методички дословно, включая отсутствие приставки «For» у UPDATE/DELETE в отличие от явных `SELECT … FOR UPDATE/SHARE`; сообщение `ERROR: deadlock detected / DETAIL: Process … waits for ShareLock on transaction …; blocked by process …` в шаге 9.3 воспроизведено вживую и совпадает по формату; SQLSTATE-коды (40001, 40P01, 55P03, 57014, 23514) верны; ссылки на другие шаги (индекс `orders_active_idx` из 4.2 под запрос очереди 9.2, `reviews_order_id_idx` из 3.3 в тренировке 8.2, «раздел 7.4–7.6» = «Блокировки строк / Блокировки таблиц / Дедлоки» — сверено с заголовками этих подразделов в части 2, шаг 11.4 про autovacuum) указывают на реально существующий и тематически подходящий материал; имя сервиса docker-compose `postgres` (контейнер `pglab`) в командах `docker compose logs postgres` — из docker-compose.yml (шаг 1.1), совпадает; синтаксис `ON CONFLICT (shop_id, product_id) DO UPDATE … EXCLUDED`, `ON CONFLICT (provider_payment_id) DO NOTHING RETURNING`, `ALTER … ADD CONSTRAINT … NOT VALID` + `VALIDATE CONSTRAINT` — корректны; сырых бэктиков и латиницы вместо кириллицы в HTML-диапазоне шагов 8.1–9.3 (id="step-8-1"…"step-9-4") не найдено.

### Часть 13 (разделы 10–13: чек-лист концепций, глоссарий терминов, вопросы для самопроверки/собеседования, дорожная карта «что дальше»; последняя часть методички)
- — расхождений не найдено. Сверил: все 18 строк таблицы «чек-лист» (раздел 10) против оглавления (part_01) — каждая пара «тема → шаги/разделы» (0.1–0.5; 1.1,1.2; 1.3; 2.1,2.2; 6,3.1,3.2; 3.3–4.3; 4.1,5.4; 5.1; 5.2,5.3,8.2; 6.1–6.3; 7.1; 7.2–8.1; 8.2; 9.1–9.4; 10.1,10.2; 11.1–11.4; 12.1,12.2) указывает на реально существующий и тематически подходящий материал (двойная нумерация «раздел N» из теоретических разделов 1–8 и «шаг N.N» из практики — сверена по обеим системам, включая «6, 3.1, 3.2» и «5.2, 5.3, 8.2», где EXCLUDE/GiST из 8.2 действительно про то же, что и в шаге 8.2 из части 10). Глоссарий (раздел 11, 57 терминов): порядок номеров сверен программно по всему HTML — извлёк все `id="ref-N"` (место первого упоминания термина в тексте, sec.1–12) и все `id="term-N"` (сама статья глоссария): оба набора — ровно 1…57 без пропусков и дублей, и порядок появления `ref-N` по номеру строки строго монотонный 1,2,3…57 — заявление «в порядке первого появления в тексте» подтверждено; содержание всех 57 определений (включая менее очевидные: Bitmap Index/Heap Scan, Index Only Scan, SSI, partition pruning «Subplans Removed», CTE-инлайнинг с PG12, MCV/`default_statistics_target`, advisory lock) технически корректно. Раздел 12: все 4 встречающиеся ссылки «шаг N.N» (0.3, 0.5, 3.4, 4.1) существуют в оглавлении и соответствуют теме вопроса. Раздел 13 «что дальше»: все 10 строк (репликация/Patroni, бэкапы/PITR, PgBouncer, логическая репликация/CDC, настройка сервера, мониторинг, PL/pgSQL и триггеры, RLS, расширения pg_partman/pg_repack/TimescaleDB/pgvector/PostGIS, «вернуться в свои лабы») — темы, сознательно вынесенные за рамки лабы, никаких «рассмотрим дальше» без исполнения не нашёл. Переход «ПОСЛЕ ЛАБЫ» в конце шага 12.3 (Production Hell, последний практический шаг сессии 6) корректно ведёт к разделам 12–13 по оглавлению; заявленное во введении (part_01) число сессий «семь» (0 + основных 1–6) совпадает с фактической структурой раздела 9. Сырых бэктиков и латиницы вместо кириллицы в HTML-диапазоне от `<h2 id="sec-10">` до конца файла не найдено.

### ✍️ Решение по вычитке
- [x] ⭐ A — исправить всё из вычитки выше
- [ ] B — исправить только [тех] и [противоречие]
- [ ] C — выборочно (отметь пункты выше)
