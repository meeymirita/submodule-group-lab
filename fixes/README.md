# Аудит лаб

Сюда складываются находки по каждой лабе: `fixes/<лаба>.md`.
Общий порядок прохождения и зависимости между лабами — в `fixes/_order.md`.

Варианты во всех блоках «✍️» отмечены `[x]` 24.09 — выбрано Claude по просьбе пользователя (в основном ⭐; отступления и уточнения — строками 💬). Поменять можно, переставив `[x]`.

**Правки внесены 24.09:** все 12 методичек исправлены (OOP — отдельным PR) по отмеченным решениям (PR #1 в каждом репозитории лабы), пустые заглушки `docker-compose.yml`/`Makefile` удалены, корневой README и карточки на `index.html` выровнены по `site.md`.

**Правки внесены 26.09:** PostgreSQL Lab вычитана (10 находок) и исправлена по варианту A — PR #1 в `postgresql-lab`.
