# Laravel Lab (TaskFlow) — находки

Методичка: `laravel/Laravel_Lab_TaskFlow.html` (~300 КБ, 16 разделов, 10 сессий). Битых якорей в оглавлении нет.
Стек в README, карточке и методичке совпадает: Laravel 13 (PHP 8.4), PostgreSQL 17, Redis 7, RabbitMQ 4, Mailpit, Reverb, Vue 3.

## 🟡 README и методичка расходятся по зависимостям
README: «нужен… ООП на PHP (см. oop-lab)… лаба на них ссылается, а не объясняет заново».
В методичке RabbitMQ-лаба упоминается 10 раз, а **OOP-лаба — ни разу**. Либо добавить в методичку 1–2 отсылки
(например, в разделе 3 про контейнер: «Decorator/Strategy из OOP-лабы…»), либо убрать из README фразу «ссылается».

## 🟡 Пакет RabbitMQ-драйвера
Используется `vladimir-yuldashev/laravel-queue-rabbitmq`. Этот пакет обычно выпускает поддержку новой мажорной
версии Laravel с задержкой. Перед стартом стоит проверить, что есть релиз с поддержкой Laravel 13
(`composer require` сам скажет). Если нет — в методичке нужен запасной путь. (Проверить из офлайна я не могу.)

## 🟡 Номера разделов, на которые ссылается «Чистый PHP»
PHP-лаба ссылается на «раздел 5 Laravel-лабы» про Container. Здесь Container — **раздел 3** (теория) и **сессия 5**
(практика). Править лучше в PHP-лабе (см. `fixes/php.md`), здесь ничего менять не надо.

## 🟢 Версии
- «auto-discovery listeners с Laravel 11+» — верно.
- Образы: `php:8.4-cli`, `postgres:17`, `redis:7`, `rabbitmq:4-management`, `axllent/mailpit`, `node:22` — рабочие.
  Postgres здесь 17, а в Redis/NestJS/Traefik — 16 (см. `fixes/site.md`, «разнобой версий»).


---

## ✍️ Варианты решений

Оставь в каждом вопросе **один** вариант (остальные удали или поставь `[x]` у нужного).
Потом я прочитаю этот файл и перепишу лабу ровно по выбранному. ⭐ — моя рекомендация.

### 1. README говорит, что методичка «ссылается» на OOP-лабу, а в методичке ссылок нет
- [x] ⭐ A — добавить в методичку 1–2 отсылки к OOP-лабе (контейнер, паттерны Decorator/Strategy)
- [ ] B — убрать «ссылается» из README, оставить OOP-лабу просто как рекомендацию

### 2. Пакет `vladimir-yuldashev/laravel-queue-rabbitmq` и Laravel 13
- [x] ⭐ A — добавить в сессию 1 проверку «если composer ругается на версию — вот запасной путь» (свой минимальный драйвер через php-amqplib, как в RabbitMQ-лабе)
- [ ] B — оставить, проверю сам при старте

### 3. PostgreSQL 17 (в других лабах 16)
- [x] ⭐ A — как решишь в `site.md` (вопрос 1)
- [ ] B — отдельно: оставить 17

---

## 📖 Вычитка методички

### Часть 1 (разделы 1–11, теория)
- — сверено: карта компонентов (раздел 1) vs таблица «Слой/Классы/Сессия» (раздел 1) — номера сессий (1,4,5,1–3,3–4,6,7,8–9,9,10) соответствуют оглавлению и реальным заголовкам сессий 1–10; раздел 4 (Eloquent/pivot) vs раздел 11 (домен TaskFlow) — имена таблиц/колонок (workspace_user+role, task_user+assigned_at, label_task, attachments morph attachable=Task|Comment, activities morph subject) совпадают; раздел 6 (middleware-пример) vs раздел 11 (структура app/Http/Middleware/EnsureWorkspaceRole) — не противоречат; раздел 8 (очереди) vs раздел 9 (RabbitMQ-драйвер) — согласовано. Расхождений не найдено.

### Часть 2 (раздел 11 продолжение, раздел 12 — Сессия 1 целиком, Сессия 2 шаги 2.1–начало 2.2)
- **[тех] Шаг 1.2, «ОЖИДАЕМЫЙ РЕЗУЛЬТАТ»** — «16 таблиц (считая users, personal_access_tokens, jobs/failed_jobs/job_batches, notifications — их создали install:api и стандартные миграции)» → (1) `notifications` не создаётся ни `install:api`, ни стандартными миграциями Laravel — нужна отдельная `php artisan notifications:table` (в лабе она не выполнялась, а уведомления появляются только в сессии 8); (2) даже без notifications подсчёт не сходится: 11 своих таблиц (workspaces, workspace_user, projects, tasks, task_user, labels, label_task, comments, attachments, activities, invitations) + users + personal_access_tokens + jobs + failed_jobs + job_batches = 16, но тогда notifications лишний в перечислении (даёт 17); кроме того, не упомянуты реально создаваемые стандартными миграциями password_reset_tokens, sessions, cache, cache_locks (это даёт ещё +4, т.е. фактически 20 таблиц). → исправить: либо убрать notifications из перечисления и явно упомянуть password_reset_tokens/sessions/cache/cache_locks, либо пересчитать итоговое число.
- **[текст] Раздел 12, Шаг 1.2, комментарий про task_user/label_task** — «У task_user и label_task, наоборot, композитный PK» → латиница «ot» вместо кириллического «от» (опечатка подтверждена и в исходном HTML). → исправить на «наоборот».
- — остальное в части 2 проверено: docker-compose.yml (образы/порты/profiles) vs env-переменные (RABBITMQ_*, REVERB_*, DB_*) vs config/queue.php (RABBITMQ_QUEUE, ключи hosts/options) — согласованы; profiles scheduler/reverb/web помечены «включим в сессии 8/9/6» и совпадают с темами этих сессий по оглавлению; имена моделей/миграций (Workspace, Project, Task, Comment, Label, Attachment, Activity, Invitation + workspace_user/task_user/label_task) из Шага 1.2 совпадают с use в Шаге 2.1 (Model::preventLazyLoading, casts для Task); PHP-синтаксис миграций (b18.php), enum'ов Role/TaskStatus/TaskPriority (b17.php), моделей Workspace/Project/Task (b20.php) — `php -l` без ошибок. Других расхождений не найдено.

### Часть 3 (сессия 2, шаги 2.2–2.8)
- **[противоречие]/[тех] Шаг 2.4, belongsToMany** — код и тинкер прямо показывают, что `Workspace::first()->members()->getTable()` без явного имени возвращает `"user_workspace"` (Eloquent берёт единственное число обеих моделей «в алфавитном порядке»). Но абзац «Зачем» тут же утверждает: «в вашей схеме имя `workspace_user` совпадает с конвенцией лишь случайно для этой пары (`workspace < user` по алфавиту — ok)» — это неверно и противоречит только что показанному коду: по алфавиту `user` < `workspace` (u раньше w), поэтому конвенция даёт именно `user_workspace`, а не `workspace_user`, что и было продемонстрировано как раз абзацем выше как ошибка угадывания. Исправить: убрать «ok»/«совпадает» и явно сказать, что `workspace_user` НЕ соответствует конвенции (отсюда и нужен явный второй аргумент), заменить «workspace < user» на «user < workspace».
- **[тех] Шаг 2.6, «Зачем»** — опечатка `típично` (латинская í вместо кириллической «и») в фразе «форма «выбрать метки чекбоксами» — típично для sync». Исправить на «типично».
- — остальное по части 3 (модели/связи `Workspace::projects/owner`, `Project::workspace/tasks`, `Task::project/creator`, SQL-примеры N+1 (1+7×2=15, eager loading 3 запроса), имена pivot-таблиц `workspace_user`/`task_user`/`label_task` против миграций сессии 1, `Membership`-класс и `$incrementing=true` (в migration `workspace_user` есть `$t->id()`), `hasManyThrough(Task::class, Project::class)` против `tasks.project_id`/`projects.workspace_id`, `morphs('attachable')`/`morphTo()`, `morphMap`) — сверено с частью 2 (модели/миграции сессии 1–2) и логикой Eloquent, расхождений не найдено.

### Часть 4 (сессия 3 целиком: 3.1–3.3; сессия 4: 4.1–4.2, начало 4.3 без «Итог» — хвост сессии 4 физически лежит в part_05.txt, это только разбивка файлов вычитки, не методички)
- **[тех] Шаг 3.2, UserResource** — `whenPivotLoaded('workspace_user', fn () => $this->membership->role->value)` не будет работать даже для «настоящих» участников воркспейса: в шаге 2.5 связь `Workspace::members()`/`User::workspaces()` явно переименовывает pivot-аксессор через `->as('membership')`, а `whenPivotLoaded()` без второго аргумента-аксессора проверяет по умолчанию именно `$this->pivot` (это `whenPivotLoadedAs('pivot', ...)`). Поскольку аксессор `pivot` для этой связи больше не существует (переименован в `membership`), условие всегда ложно — поле `role` не появится никогда, даже если ресурс когда-то используют для списка участников воркспейса, а не только для `assignees`. Нужно `whenPivotLoadedAs('membership', 'workspace_user', ...)`.
- **[тех]/[противоречие] Шаг 4.1, StoreTaskRequest** — код: `Rule::exists('labels', 'id')->where('workspace_id', fn () => $this->route('project')->workspace_id)` — двухаргументная форма `where(column, value)`, где `value` — замыкание. `DatabaseRule::where()` не резолвит замыкание, переданное вторым аргументом (лениво выполняется только форма с ОДНИМ аргументом-замыканием: `->where(fn ($query) => ...)`, которая уходит в `using()`); переданный как значение `Closure` уйдёт в биндинг SQL как есть и приведёт к ошибке/неверному сравнению, а не подставит `workspace_id`. Это же расхождение видно и в самой методичке: блок «ПРОВЕРЬ СЕБЯ» тут же описывает этот код иначе — как `Rule::exists(...)->where(fn () => ...)` (однoаргументная форма) — то есть вопрос не соответствует показанному чуть выше коду. Исправить код на `Rule::exists('labels', 'id')->where(fn ($query) => $query->where('workspace_id', $this->route('project')->workspace_id))`.
- ~~**[тех] Шаг 4.2, routes/api.php** — второй `Route::delete('/tasks/{task}')` недостижим~~ — **снято при сверке:** в Laravel `RouteCollection` хранит маршруты по ключу «метод + домен + URI», и повторная регистрация `DELETE v1/tasks/{task}` (shallow-destroy из `apiResource` даёт тот же URI в той же группе) **перезаписывает** первый маршрут — комментарий «перекроет предыдущий route» верен. Можно (необязательно) сделать явнее: `->except('destroy')` у `apiResource` и отдельный `Route::delete(...)->name('tasks.destroy')`, чтобы не терять имя маршрута.
- **[текст] Шаг 4.1, ПРОВЕРЬ СЕБЯ** — опечатка «Что произойдёt» (латинская t вместо «т»).
- — остальное по части 4 (коллекции 3.1 — `groupBy`/`partition`/`countBy`/`keyBy`/`reduce`/`when` на моделях `Workspace`/`Task` из сессии 2; ресурсы 3.2 — `TaskResource`/`LabelResource`/`whenLoaded`/`whenCounted` против связей `creator/assignees/labels`/`withCount('comments')`; пагинация 3.3 — `paginate`/`simplePaginate`/`cursorPaginate`, HTTP-статусы, `TaskFactory`; CRUD 4.1 — `StoreTaskRequest`/`UpdateTaskRequest`/`TaskController`, коды 201/422/404; middleware 4.2 — `EnsureWorkspaceRole`, алиас, порядок `auth:sanctum`) — сверено с частями 1–3 и синтаксисом PHP 8.4/Laravel 13, расхождений не найдено.

### Часть 5 (сессия 4, окончание шага 4.3; сессия 5 целиком: 5.1–5.3; сессия 6, начало: 6.1–6.2)
- **[противоречие] Раздел 7 (теория, часть 1, таблица Guard/Provider/Sanctum) vs Шаг 6.1** — раздел 7: «Guard … auth:sanctum — Bearer-токен для API и Vue». Шаг 6.1 прямо противоречит этому: «Login/logout через Sanctum SPA-режим (cookie-based, а не «токен в localStorage»)… это безопаснее токена в localStorage… и ровно то, что нужно для Vue-фронта на том же порту». Vue-фронт в лабе (6.1–6.3) работает через cookie/CSRF-сессию, а не Bearer-токен. Исправить формулировку в разделе 7: «auth:sanctum — Bearer-токен для внешних API-клиентов (curl/сессии 1–5), cookie+CSRF (stateful) — для Vue-фронта (сессия 6)».
- **[тех] Шаг 6.1** — код помечает Sanctum SPA-режим настроенным комментарием «// config/sanctum.php — уже настроено install:api; проверить stateful домены» и сразу переходит к контроллеру/curl-тестам. Не хватает обязательного шага для Laravel 11+/13: регистрации `EnsureFrontendRequestsAreStateful` для группы `api` — `->withMiddleware(fn ($middleware) => $middleware->statefulApi())` в `bootstrap/app.php` (или добавление мидлвари вручную). `php artisan install:api` этого не делает сам по себе — без этого шага запросы с фронта не будут распознаны как «stateful», и cookie-сессия/CSRF из 6.1–6.3 работать не будет (пользователь получит `Unauthenticated.` вместо ожидаемого `{"user": {...}}`). Исправить: добавить в 6.1 явный код `$middleware->statefulApi();` в `bootstrap/app.php`.
- **[тех] Шаг 6.1, curl-пример** — `CSRF=$(grep XSRF cookies.txt | awk '{print $NF}')` берёт значение cookie `XSRF-TOKEN` как есть из `cookies.txt`, но Laravel отдаёt его URL-encoded (шифротекст содержит `+`, `/`, `=`, которые кодируются). Axios (`withXSRFToken: true`, применяется в 6.3) сам делает `decodeURIComponent()` перед тем как положить значение в заголовок `X-XSRF-TOKEN` — именно поэтому там «просто работает». В curl-примере же decode не делается, и с большой вероятностью POST `/api/login` вернёт `419 Page Expired` вместо документированного результата. Исправить: `CSRF=$(grep XSRF cookies.txt | awk '{print $NF}' | php -r 'echo urldecode(fgets(STDIN));')` (или аналогичный urldecode).
- ~~**[тех] Шаг 6.2, TaskPolicy::update()** — `$task->assignees` упадёт с `LazyLoadingViolationException`~~ — **снято при сверке:** `preventLazyLoading` в Laravel срабатывает только для моделей, загруженных в коллекции из >1 записи; в политику `Task` приходит одной моделью через route model binding, так что исключения не будет (иначе падал бы и `$task->project` в той же строке).
- — остальное по части 5 проверено: обработка исключений 4.3 (`WorkspaceLimitExceeded`, `withExceptions`, порядок `render()`, `dontReport`) — синтаксис и порядок регистрации корректны, `app()->hasDebugModeEnabled()` — реальный метод; Container 5.1–5.3 (`build()`, `bind`/`singleton`, `app()->call()`, `TaskNotifier`/`LogTaskNotifier`/`MailTaskNotifier`/`TaskAssigner`, `ActivityLogger`/`DatabaseActivityLogger`/`NullActivityLogger`, contextual `when/needs/give`) — имена классов/методов согласованы между шагами и с `Activity`/`activities` (полиморфная таблица из части 2–3), `TaskAssignmentController`/маршрут `/tasks/{task}/assignees` и мидлварь `workspace.role:member` — согласованы с `EnsureWorkspaceRole` (часть 4, умеет резолвить workspace через `task->project->workspace`); Policy 6.2 (`TaskPolicy`/`ProjectPolicy`/`WorkspacePolicy`, `before()`, автообнаружение по имени класса, `Gate::authorize`) — сигнатуры и вызовы `$this->authorize()`/`Gate::authorize()` соответствуют методам политики; `Role`/`isAtLeastIn` — согласованы с частью 3. Других расхождений не найдено.

### Часть 6 (сессия 6, окончание: 6.3–6.4; сессия 7: 7.1–7.3 частично)
- **[тех] Шаг 6.3, маршрутизация Vue (главный баг части)** — три файла не согласованы друг с другом: `WorkspacesView.vue` формирует ссылку `RouterLink :to="`/workspaces/${w.id}/board`"` без query-параметра; `router/index.js` объявляет `{ path: '/workspaces/:id/board', component: BoardView, props: r => ({ projectId: r.query.project }) }` — то есть ждёт `projectId` из query-строки `?project=`, которой ссылка не передаёт; а `BoardView.vue` вообще не объявляет `defineProps(['projectId'])` и вместо этого читает `route.params.projectId`, которого не существует — путь содержит только `:id` (это workspace id, а не project id). В результате `load()` всегда шлёт `GET /api/v1/projects/undefined/tasks`, и описанный в «ОЖИДАЕМЫЙ РЕЗУЛЬТАТ» «полный цикл: логин… список воркспейсов, доска с фильтром по статусу» не воспроизводится как есть. Кроме того, смешаны понятия: ссылка идёт по id воркспейса, а `BoardView` ожидает id проекта (в схеме TaskFlow это разные сущности, воркспейс → проекты → задачи) — навигация «воркспейс → доска проекта» вообще не показывает выбор проекта. Исправить: либо добавить во `WorkspacesView` список проектов воркспейса и ссылку `/workspaces/{w.id}/board?project={p.id}`, либо переименовать route-параметр в `:projectId` и убрать лишний слой `props`/`query`, и в `BoardView.vue` явно объявить `const props = defineProps(['projectId'])` и использовать `props.projectId` вместо `route.params.projectId`.
- — остальное по части 6 проверено: 6.4 (`WorkspaceInviter`, `Invitation`/`invitations` HasMany на `Workspace`, `StoreInvitationRequest::authorize()` через `can('invite', ...)` — согласовано с упражнением в 6.2 про `WorkspacePolicy::invite`, `{invitation:token}` binding по колонке `token`) — имена классов/методов согласованы; 7.1 Observer (`TaskObserver`, `#[ObservedBy]`, `created/updating/deleted`, `isDirty`/`getOriginal`, ловушка массовых `Query Builder`-операций) — соответствует `Activity`/`activities` из частей 2–3; 7.2 Event+Listeners (`TaskAssigned`, `SendAssignmentNotification` (ShouldQueue) и `NotifyWatchers` (sync), `SerializesModels`, `event:list` auto-discovery, замена прямого вызова `TaskNotifier`/`ActivityLogger` в `TaskAssigner::assign()` на `TaskAssigned::dispatch`) — согласовано с 5.2/5.3 и с очередью `rabbitmq`/сервисом `queue` из docker-compose (часть 2); 7.3 Job (`RecalculateProjectStats`, `ShouldBeUnique`/`uniqueId()`, `$tries`/`$backoff`, `failed()`, `queue:failed`) — сигнатуры и артisan-команды корректны, имя сервиса `queue` в `docker compose logs queue` соответствует docker-compose (часть 2). Расхождений, кроме отмеченного выше, не найдено.

### Часть 7 (шаги 7.4–8.3: queue:work изнутри, Mailable, Notification, Scheduler)
- **[текст] Шаг 8.1, «Зачем»** — «markdown: в Content даёt готовую тему письма» → латинская «t» вместо «т» → исправить на «даёт».
- **[текст] Шаг 8.2, «Проверь себя»** — «Добавьte канал broadcast в via()» → латинские «te» вместо «те» → «Добавьте».
- **[тех] Шаг 8.1/8.2** — `WorkspaceInvitationMail::content()` и `TaskAssignedNotification::toMail()` используют `config('app.frontend_url')`, но эта настройка нигде в методичке не определена (нет ни строки в `config/app.php`, ни `FRONTEND_URL` в `.env` из сессии 1.1) → по умолчанию вернёт `null`, ссылки в письмах будут вида `/invitations/<token>` без домена → добавить `'frontend_url' => env('FRONTEND_URL', 'http://localhost:5173')` в `config/app.php` и переменную в `.env` сессии 1.
- **[тех] Шаг 8.2** — curl `GET localhost:8000/api/v1/notifications` подразумевает маршруты `NotificationController::index/markRead`, но их регистрация (`Route::get/patch(...)`) нигде в тексте не показана — в отличие от аналогичного `ProjectStatsController` в 9.1, где `Route::get(...)` явно приведён → добавить строки маршрутов в `routes/api.php` рядом с текстом 8.2.
- **[противоречие] Шаг 8.2** — фраза «теперь MailTaskNotifier / LogTaskNotifier / интерфейс TaskNotifier из сессии 5 можно удалить» повторяет то, что уже было сказано в шаге 7.2 («TaskNotifier и его биндинг теперь можно убрать — их роль взяли на себя listeners»); методичка не отмечает, что это уже сделано, и предлагает «удалить» уже удалённое → в 8.2 либо убрать повтор, либо явно сослаться на 7.2.
- — остальные шаги 7.4, 8.1–8.3 (команды artisan, docker compose, ожидаемый вывод, имена классов Mailable/Notification/SendDailyDigest, маршруты, env-переменные RabbitMQ/Mailpit) сверены с состоянием проекта на этом этапе (сессии 1–7), расхождений, кроме перечисленных, не найдено.

### Часть 8 (шаги 9.1–10.4: Cache, Cache::lock, RateLimiter, Broadcasting, фабрики, тесты, финал)
- **[тех] Шаг 9.1** — в `TaskObserver` добавляется метод `deleted(Task $task): void { Cache::forget(...); }`, но метод `deleted()` в этом классе уже объявлен в шаге 7.1 (`$this->activity->log($task, 'deleted');`) → повторное объявление метода с тем же именем в одном классе — фатальная ошибка PHP («Cannot redeclare method»). Нужно объединить оба действия в одном теле `deleted()`, методичка об этом не говорит.
- **[тех] Шаг 9.1** — команда `docker compose exec redis redis-cli GET "laravel_database_project:1:stats"` (и следом `EXISTS ...`) использует префикс `laravel_database_`, тогда как реальный префикс Redis-кэша Laravel — `laravel_cache_...`; в шаге 9.3 для того же Redis-хранилища кэша (RateLimiter) корректно используется `laravel_cache*task-creation*` → несоответствие префикса внутри одной сессии, в 9.1 нужно `laravel_cache_project:1:stats`.
- **[тех] Шаг 9.3** — `Route::post('/projects/{project}/tasks', [TaskController::class, 'store'])->middleware(['auth:sanctum', 'throttle:task-creation'])` написан вне группы `prefix('v1')`, где живёт `apiResource('projects.tasks')` (шаг 4.2) → это **другой** URI (`/api/projects/{project}/tasks` вместо `/api/v1/projects/{project}/tasks`): фронт и curl-примеры ходят на `v1`, и лимит `task-creation` к ним не применяется. Если же вставить строку внутрь группы `v1`, она **перезапишет** store-маршрут ресурса (Laravel хранит маршруты по «метод + URI») и потеряет `workspace.role:member`. (Уточнено при сверке: агент писал, что «матчится первый зарегистрированный» — это неверно, но проблема реальна.) → Исправить: вместо отдельного маршрута добавить throttle к store ресурса — `Route::apiResource(...)->middlewareFor('store', 'throttle:task-creation')` (Laravel 12+) или `->except('store')` + отдельный `Route::post(...)` внутри группы `v1` с обоими middleware.
- **[противоречие] Шаг 10.3** — `TaskAssignerTest::test_assign_attaches_user_and_records_activity` мокает `ActivityLogger`, ждёт `shouldReceive('log')->once()` с action `'assigned'` при вызове `TaskAssigner::assign($task, $user, $task->creator)`. Но в шаге 7.2 `TaskAssigner::assign()` переписан так, чтобы **не** вызывать `notifier`/`activity` напрямую («ОДНА строка вместо ->notifier->notifyAssigned() и ->activity->log()»), а только диспетчерить `TaskAssigned::dispatch(...)`; ни один из listener'ов события (`SendAssignmentNotification`, `NotifyWatchers`, `RecalculateStatsOnTaskChange`) не вызывает `ActivityLogger`. Тест написан под старую (сессия 5.3) версию `assign()` и с текущей реализацией не пройдёт — ожидание мока не выполнится.
- **[тех]/[противоречие] Шаг 10.4** — карта проекта перечисляет `Controllers/Api/{V1/{Task,Project,Workspace,Invitation,ProjectStats}Controller, ...}`, но `ProjectController` и `WorkspaceController` нигде в методичке не создаются (`make:controller` вызывался только для `TaskController`, `AuthController`, `InvitationController`; `ProjectStatsController`/`NotificationController` добавлены отдельно) — в шаге 6.3 эндпоинт `GET /api/v1/workspaces` прямо оставлен студенту как самостоятельное упражнение («добавьте GET /v1/workspaces сами»), то есть `WorkspaceController` формально не существует к моменту 10.4.
- — остальное по шагам 9.2, 9.4, 10.1, 10.2 (Cache::lock/WorkspaceInviter, Broadcasting/Reverb/Echo, фабрики WorkspaceFactory/ProjectFactory/TaskFactory, Feature-тест TaskApiTest) сверено с моделями/маршрутами/политиками сессий 1–6, расхождений, кроме перечисленных, не найдено.

### Часть 9 (разделы 13–16: чек-лист, глоссарий, вопросы, «Что дальше»)
- **[текст] Раздел 13, строка «Коллекции»** — «3.1 logика над уже загруженными данными» → латинская «logика» вместо «логика».
- — остальное: соответствие номеров сессий/шагов в таблице чек-листа реальным заголовкам шагов (2.1–10.3), термины глоссария — пройденным в разделах 1–10 темам, вопросы раздела 15 — соответствующим шагам, нумерация разделов 13→14→15→16 и их порядок в оглавлении (часть 1) — сверено, расхождений, кроме указанного, не найдено. Внешние ссылки (RabbitMQ-лаба, OOP-лаба, TypeScript-лаба, Filament, Scramble, Pulse, stancl/tenancy) не проверялись по инструкции.

### ✍️ Решение по вычитке
- [x] ⭐ A — исправить всё из вычитки выше
- [ ] B — исправить только [тех] и [противоречие]
- [ ] C — выборочно (отметь пункты выше)
