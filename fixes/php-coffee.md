# OOP Lab (php-coffee, Coffee Shop API) — находки

Лаба раньше не вычитывалась (была в работе у пользователя; начато только до шага 1.5 — пользователь согласен начать заново после правок).

## 📖 Вычитка методички
### Часть 1 (разделы 1–9, до шага 1.4 включительно)

- **[тех] Шаг 1.1, docker-compose.yml, комментарий про RabbitMQ-пользователя** — комментарий гласит: «# load_definitions (rabbitmq.conf) — пользователя cafe/cafe создаёт только # блок "users"/"permissions" в definitions.json ниже. Без него — Not_Authorized...», но показанный чуть выше в том же шаге код `rabbitmq/definitions.json` содержит только `vhosts`, `exchanges`, `queues`, `bindings` — блоков `"users"`/`"permissions"` в нём нет. Получается, методичка сама описывает необходимость этого блока, но не приводит его в коде: следуя шагу 1.1 буквально, пользователь cafe/cafe в RabbitMQ не будет создан, вход в UI (`http://localhost:15672`, cafe/cafe), заявленный в «ожидаемом результате» этого же шага, закончится Not_Authorized. (Для сравнения: в реальном репозитории `/home/user/submodule-group-lab/php-coffee/rabbitmq/definitions.json` такой блок `"users"/"permissions"` присутствует — значит, кто-то добавил его вручную сверх того, что описано в методичке.) Исправление: добавить в пример `definitions.json` шага 1.1 блоки `"users": [{"name":"cafe","password":"cafe","tags":["administrator"]}]` и `"permissions": [{"user":"cafe","vhost":"/","configure":".*","write":".*","read":".*"}]`.

- **[тех] Шаг 1.2, эксперимент (в) «количество: qty => -2»** — заявленный «ожидаемый результат»: «Итого: 150 ₽ ← −920 + 150; тоже молча». Арифметика в самой подсказке не сходится: −920 + 150 = −770, а не 150. Реальный запуск кода методички (itemPrice/orderTotal с латте L, extras oat+shot, qty=-2, плюс эспрессо S, qty=1) подтверждает: `php lab0/01_arrays.php` с этими данными печатает «Итого: -770 ₽», а не «Итого: 150 ₽». Исправление: заменить заявленный вывод на «Итого: -770 ₽ ← −920 + 150».

- **[противоречие] Раздел 1, вводная таблица «пять вопросов»** — строка «Оплатить заказ: наличные, карта, бонусы» описывает решение как «три класса, реализующих PaymentMethod; в тесте подставляем четвёртый, поддельный». Однако по структуре проекта (раздел 7: `Payment/ PaymentMethod + CashPayment, CardPayment; CardGateway (interface); PaymentType; Receipt; PaymentFailed`) в основной лабе строится только два класса — `CashPayment` и `CardPayment`. `PaymentType::Bonus` и `BonusPayment` упоминаются лишь один раз во всём документе — как необязательное самостоятельное упражнение «Сделайте сами» в шаге 4.3, а не как часть демонстрируемого решения. Заявление «три класса» во вводном примере вводит в заблуждение относительно того, что реально строится по шагам. Исправление: либо изменить пример на «наличные, карта» (два класса), либо явно пометить бонусы как задание «сделайте сами» уже во вводной таблице.

- **[противоречие] Раздел 6, таблица паттернов, строка Value Object** — колонка «Сессия» указывает «1–2», а колонка «В проекте» содержит `Money, OrderLine, Bill, Receipt`. Но `Bill` и `Receipt` по структуре проекта (раздел 7: `Pricing/ ... Checkout, Bill`, `Payment/ ... Receipt`) и по названию шага 4.2 («Checkout + PaymentMethod + /pay») создаются в сессии 4, а не в сессии 1–2. Исправление: расширить диапазон сессий до «1–4» либо убрать Bill/Receipt из этой ячейки (оставить только Money, OrderLine).

Проверено (без расхождений): TOC (разделы 1–13, подпункты 2.1–2.13, 3.1–3.4, шаги 1.1–5.4) совпадает с заголовками `<h2 id="sec-N">`/`<h3>`/`Шаг N.N` по всему HTML-источнику. Все runnable-фрагменты раздела 2 (2.1–2.10, 2.3, 2.4, 2.6, 2.7) выполнены через `php -r`/файлом на PHP 8.4.19 — фактический вывод совпадает с заявленным (330, ошибки readonly/private/ArgumentCountError/ValueError и т.д.). Полностью пересобраны и запущены `lab0/01_arrays.php` (несломанный кейс: 1070 ₽ — совпадает), `Money.php`+`02_money.php` (все 5 строк вывода и 3 попытки сломать совпадают), `Drinks.php`+`03_drinks.php` (цены 150/330/270/200 ₽ и все 3 попытки сломать — совпадают). Пути/имена файлов в дереве проекта (раздел 7) совпадают с `lab0/*.php` в `laravel-app/`. `docker-compose.yaml`, `rabbitmq/rabbitmq.conf`, `laravel-app/Dockerfile` в репозитории лабы совпадают с кодом методички дословно (кроме недостающего блока users/permissions в definitions.json, см. выше). Сквозная нумерация SOLID-таблицы (раздел 8) и таблицы паттернов (раздел 6) — ссылки на разделы/шаги (2.3, 3.2, 4.1 и т.п.) соответствуют реальным заголовкам шагов. Отсутствуют сырые бэктики (0 вхождений `` ` `` в HTML) и смешение латиницы/кириллицы внутри слов.

### Часть 2 (разделы 9, шаги 1.1–1.4, продолжение проверки той же части текста, полученной агенту A вторым файлом part_02.txt)

Файл part_02.txt (по границе разбивки на части) содержит тот же текстовый диапазон, что и часть уже описанного выше материала (раздел 7 «Стек, структура проекта» — раздел 9 до середины шага 1.4, обрыв на границе байт внутри блока Drinks.php перед файлом 03_drinks.php). Отдельных новых ошибок, кроме перечисленных выше в «Часть 1», в этом диапазоне не найдено. Все находки объединены в блок «Часть 1» во избежание дублирования между частями с наложившимися границами.

### Часть 6 (Шаги 5.1–5.4, начало раздела 10 «Чек-лист»)

- **[тех] Шаг 5.2, демо-команда** — «CAFE_NOTIFIER=mail docker compose --profile workers up -d --build» → переменная окружения задаётся только в shell перед вызовом `docker compose`, а `docker-compose.yaml` (проверено в репо) не пробрасывает её в контейнер: у сервиса `app` (и у `barista-worker`/`notify-worker`, наследующих `&app`) есть только `env_file: ./laravel-app/.env`, ни `environment: [CAFE_NOTIFIER]`, ни `${CAFE_NOTIFIER}` в файле нет. Значит `config('cafe.notifier')` внутри контейнера не изменится, и описанный «ОЖИДАЕМЫЙ РЕЗУЛЬТАТ» (письмо в Mailpit) не воспроизведётся → нужно либо прописать `CAFE_NOTIFIER=mail` прямо в `laravel-app/.env` и пересобрать/пересоздать контейнер (`docker compose up -d --force-recreate`, как делается для CAFE_DISCOUNT в шаге 4.х), либо добавить `environment: - CAFE_NOTIFIER` в `docker-compose.yaml`.
- **[противоречие] Шаг 5.4, финальная карта — «app/Providers/AppServiceProvider 7 биндингов»** → по всей методичке явных `$this->app->bind(...)`/`singleton(...)` ровно 6: `OrderRepository` (bind), `Clock` (bind), `CardGateway` (bind), `DiscountPolicy` (bind), `EventPublisher` (singleton), `Notifier` (bind, шаг 5.2); седьмого нет — более того, тут же в шаге 5-текста прямо сказано, что `Checkout`, `PaymentMethodFactory`, `AmqpConnectionFactory` резолвятся контейнером БЕЗ биндинга → исправить число на 6 (или показать в тексте недостающий 7-й биндинг, если он подразумевался).
- **[противоречие] Шаг 5.4, финальная карта — «app/Domain/ ← 27 классов»** → при подсчёте по перечисленному тут же списку сущностей (Money; Menu: Drink+4 напитка, BeverageDecorator+3 добавки, BeverageBuilder; Order: Order, OrderLine, OrderNotFound; Pricing: 5 стратегий, Checkout, Bill; Payment: CashPayment, CardPayment, Receipt, PaymentFailed), не считая enum'ы (Size, DrinkType, Extra, OrderStatus, PaymentType) и интерфейсы (Beverage, OrderRepository, Notifier, DiscountPolicy, Clock, PaymentMethod, CardGateway, EventPublisher) — которые сама методичка в глоссарии (раздел 11) явно определяет как отдельные от «класса» сущности, — получается 25 классов, а не 27. Поправить число либо явно указать, что оно включает enum'ы/интерфейсы.


### Часть 7 (окончание раздела 10 «Чек-лист», раздел 11 «Глоссарий», раздел 12 «Вопросы», раздел 13 «Что дальше»)

- **[текст] Раздел 13 «Что дальше», колонка «Почитать»** — «docs/helpers#pipeline», «docs/contracts», «php.net/releases/8.4», «phpstan.org» оформлены кликабельными `<a href="...">` ссылками, а «ваш RabbitMQ Lab, раздел 5», «OOP Lab — Worker Kit», «Вернон, «Implementing DDD», гл. 5–8» и особенно «Redis_Lab_Plan.html» — просто текст без ссылки (проверено по исходнику `docs/OOP_Lab_CoffeeShop.html`: у этих ячеек нет тега `<a>`), хотя файл `redis/Redis_Lab_Plan.html` реально существует по соседству в репозитории (`/home/user/submodule-group-lab/redis/Redis_Lab_Plan.html`). Непоследовательное оформление одной и той же колонки таблицы вводит студента в заблуждение — «Redis_Lab_Plan.html» выглядит как имя файла, но не кликабельно → сделать единообразно: либо все внутренние ссылки как обычный текст с пояснением «см. соседний проект», либо действительно оформить гиперссылкой (относительным путём) там, где файл существует.

**Сверено (кросс-проверка с более ранними частями, расхождений, кроме отмеченных, не найдено):** сигнатура `EventPublisher::publish(string $event, array $payload): void` и её реализации (`AmqpEventPublisher` из шага 5.1, `RecordingEventPublisher` из шага 5.3) совпадают; имена очередей `barista.queue`/`notify.queue` и routing key `order.paid`/`order.ready` из шагов 5.1–5.2 совпадают с `rabbitmq/definitions.json` в репозитории; команды `worker:barista`/`worker:notify` и профиль `workers` в `AbstractAmqpWorker`/`docker-compose.yaml` совпадают; цены (Latte L=250+80, oat=+60, шаг 5.3 тест: (250+80+60)×2=780, −10%=702) совпадают с шагами 2–4; `Order::itemCount()`, `Order::$email` (readonly ?string), `OrderStatus::canTransitionTo` (Paid→Ready) и `OrderRepository::save()/get()` (без `find`) — совпадают с шагом 4.x и используются идентично в шагах 5.2–5.3; количество unit/feature тестов (6+1=7 классов, 23 теста) совпадает с перечислением в «ОЖИДАЕМЫЙ РЕЗУЛЬТАТ»; все id-якоря `step-5-1..4`, `sec-10..13` из сайдбара/оглавления существуют в HTML и совпадают с заголовками, извлечёнными из частей 6–7; php -l прогнан по ключевым фрагментам кода (`AbstractAmqpWorker`, `BaristaWorkerCommand`, `InMemoryOrderRepository`, `RecordingEventPublisher`) — синтаксических ошибок нет. Отдельно замечено (вне зоны части 6–7, но всплыло при сверке): в разделе 8 (SOLID/ISP, часть 2) интерфейс `OrderRepository` описан как «save и find», хотя реальный метод, определённый в шаге 4.2 и используемый во всех местах шагов 5.1–5.3 (включая `InMemoryOrderRepository` в части 7), называется `get()` — стоит передать эту находку тем, кто вычитывает часть 2/раздел 8.

