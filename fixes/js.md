# Чистый JS Lab — находки

Методичка: `js/JS_Lab_VanillaHelpdesk.html`. Битых якорей в оглавлении нет.
README, карточка и методичка согласованы: ES2022+, Node.js 22, json-server, `node --test`, без бандлера.

## 🟢 В порядке
- Ссылки на Vue Lab и TypeScript Lab — только «вперёд» (что будет дальше и как сравнить), лаба от них не зависит.
  Это правильно для лабы, которая идёт первой в frontend-ветке.
- `json-server@0` закреплён на нулевой мажорной версии — это правильно: v1 долго в бете и API у неё другой.

## 🟡 Мелочи
- Node 22 → актуальный LTS — 24. Для этой лабы разницы нет (всё, что используется, есть в 22).
- ~~У лабы нет docker-compose — так и задумано (без Docker)~~ — **это было неверно (исправлено по вычитке).**
  Методичка строит все команды на `docker compose` (сервисы dev/api/web), а `docker-compose.yml` ученик создаёт сам в сессии 1.
  Противоречие только в README лабы: там «Node.js 22 для песочниц» и ни слова про Docker.


---

## ✍️ Варианты решений

Оставь в каждом вопросе **один** вариант (остальные удали или поставь `[x]` у нужного).
Потом я прочитаю этот файл и перепишу лабу ровно по выбранному. ⭐ — моя рекомендация.

### 1. README не говорит, что лаба работает через Docker (а методичка — через `docker compose`)
- [ ] ⭐ A — дописать в README и в стек на сайте «Docker (compose-файл создаётся в сессии 1)»
- [ ] B — переписать методичку на голый `node` / `npx json-server` без Docker
- [ ] C — оставить

### 2. Версия Node
- [ ] ⭐ A — как решишь в `site.md` (вопрос 2)
- [ ] B — отдельно: оставить 22


---

## 📖 Вычитка методички

### Часть 1 (разделы 1–6.1)
- **[противоречие] Вступление «Как устроена лаба» (перед разделом 1)** — «Разделы 1-9 — теория языка и рантайма: от типичных багов без понимания фундамента до event loop, DOM и модулей.» → DOM — это раздел 12, «Модули: ESM vs CommonJS» — раздел 13, оба вне «разделов 1-9» (там event loop — раздел 9, это верно, но DOM и модули — нет) → указать реальный диапазон (например, «разделы 1-15») или переписать список тем.
- **[противоречие] Вступление «Как устроена лаба»** — «Раздел 10 — восемь сессий заданий по нарастанию: язык в Node-песочнице (сессии 1-4) → …» → по оглавлению восемь сессий — это раздел «16. Задания (8 сессий)», а раздел 10 называется «Callback → Promise → async/await» и сессий не содержит → заменить «Раздел 10» на «Раздел 16».
- **[противоречие] Вступление «Как устроена лаба»** — «Разделы 11-14 — чек-лист, глоссарий, вопросы для собеседования, что дальше.» → по оглавлению это разделы 17-20 («17. Чек-лист концепций», «18. Глоссарий», «19. Вопросы для собеседования», «20. Что дальше»); разделы 11-14 на самом деле — «fetch и AbortController», «DOM без фреймворка», «Модули: ESM vs CommonJS», «Storage и своя реактивность» → заменить «11-14» на «17-20».
- **[тех] Раздел 3.5, блок «Под капотом: object → primitive»** — «движок вызывает внутренний алгоритм ToPrimitive: сначала пробует Symbol.toPrimitive (если определён), потом valueOf(), потом toString()» → порядок valueOf/toString зависит от hint: для String(obj) (как раз только что приведённого примера) используется hint "string", и по спецификации сначала вызывается toString(), потом valueOf() — обратный порядок тому, что написано как общее правило (для Number(obj)/сложения порядок действительно valueOf→toString) → уточнить, что порядок зависит от hint («string»: toString→valueOf; «number»/«default»: valueOf→toString).
- **[тех] Раздел 4.3 «Методы массивов — какой когда», пример с tickets** — «const openTitles = tickets.filter(t => t.status === 'open').map(t => t.id); // [1, 3]» → переменная называется openTitles («заголовки»), но map возвращает t.id, то есть id, а не title — вводит в заблуждение → переименовать в openIds (или заменить .map(t => t.id) на .map(t => t.title), если нужны именно заголовки).

### Часть 2 (разделы 6.2–13)
- **[тех] Раздел 6.2 «Каррирование»** — «withStandardTax(100); // 90 — можно переиспользовать withStandardDiscount для разных price» → фактический результат 100 * (1 - 0.1) * (1 + 0.2) = 90 * 1.2 = 108, а не 90 (проверено `node -e`) → исправить комментарий на «// 108».
- — остальное в разделах 6.2–13 не найдено (примеры по прототипам/class/Symbol/Map/Set/WeakMap/event loop/Promise/fetch/AbortController/debounce-throttle/DOM мысленно и через node проверены, расхождений с ожидаемым выводом не найдено).

### Часть 3 (раздел 13 «Модули» — сессия 2, шаг 2.1)
- **[текст] Сессия 2, шаг 2.1, блок кода `03_coercion.js`** — «console.log('=== пять результатов ==, которые ломают интуицию ===');» → лишнее «==,» после слова «результатов» (бессмысленный обрывок, встречается и в коде, и в «ожидаемом результате» ниже) → убрать «==,», должно быть «=== пять результатов, которые ломают интуицию ===».
- — остальное в разделе «Модули» и в шаге 2.1 (таблица CommonJS/ESM, Storage, structuredClone, Proxy-реактивность, docker-compose/package.json сессии 1, шаги 1.2–1.3 с hoisting/TDZ, весь вывод coercion-примера кроме заголовка выше) проверено построчно и через `node`, расхождений не найдено.

### Часть 6 (шаги 7.2–8.3)
- **[тех] Шаг 7.2, проверка в консоли** — «import { saveFilters, onFiltersChangedElsewhere } from '/app/filters.js';» (предлагается ввести прямо в консоли DevTools) → статическая import-декларация не работает в консоли браузера — это не модуль, будет `SyntaxError: Cannot use import statement outside a module`; работает только динамический `import()` (проверить на актуальном Chrome/Firefox) → заменить на `const { saveFilters, onFiltersChangedElsewhere } = await import('/app/filters.js');`.
- **[тех] Шаг 8.3, `store.test.js`** — «t.mock.method(apiModule, 'updateTicketStatus', async () => { throw new Error('503'); });   // node:test встроенный мок модуля» → у объекта-неймспейса ES-модуля (результат `await import(...)`) свойства неконфигурируемы/не допускают присваивания (Module Namespace Exotic Object, [[Set]] всегда возвращает false) — `mock.method` не сможет подменить экспорт, тест упадёт с TypeError, а не пройдёт, как показано в «ожидаемом результате» → использовать `t.mock.module('../web/app/api.js', { namedExports: { updateTicketStatus: async () => {...} } })` (стабильно с Node 22.3) вместо `mock.method` на неймспейсе.
- **[текст] Шаг 8.2, «ПРОВЕРЬ СЕБЯ»** — «Сравните с тем, как вёл бы себя `v-for` с `:key` в Vue.» → инлайн-код везде в документе оформлен тегом `<code>`, здесь вместо этого сырые markdown-бэктики — в браузере отобразятся как есть, без стиля кода → заменить на `<code>v-for</code>` и `<code>:key</code>`.

### Часть 7 (шаг 8.4, разделы 17–20)
- **[тех] Шаг 8.4, таблица сравнения с Vue, строка Pinia store** — «Devtools-интеграция, computed-геттеры с кэшированием, персистентность из коробки» → Pinia не сохраняет состояние между перезагрузками «из коробки»: для этого нужен отдельный плагин (например, pinia-plugin-persistedstate), это не встроенная возможность ядра → убрать пункт или переформулировать как «персистентность через плагин».
- **[тех] Шаг 8.4, таблица сравнения с Vue, строка «Reactivity Transform / SFC компиляция»** — «Reactivity Transform / SFC компиляция ... синтаксический сахар <script setup>, автоматический unwrap ref в шаблоне, HMR» → Reactivity Transform объявлен deprecated в Vue 3.3 и удалён из ядра в 3.4 — в Vue 3.5 (та версия, по которой должно идти сравнение) этой фичи уже нет, приводить её как «то, что делает Vue» некорректно; заодно «автоматический unwrap ref в шаблоне» — это отдельная, всегда включённая возможность Vue 3, к Reactivity Transform не относящаяся → убрать строку про Reactivity Transform или заменить её актуальной фичей.
- **[противоречие] Оглавление vs заголовок раздела 17** — в меню (оглавление вверху документа): «17. Чек-лист концепций»; в самом разделе: «17. Чек-лист: концепция ↔ где она в коде» → один и тот же раздел назван по-разному → привести к одному названию.
- **[противоречие] Оглавление vs заголовок раздела 19** — в меню: «19. Вопросы для собеседования»; в самом разделе: «19. Вопросы на понимание (и для собеседования)» → один и тот же раздел назван по-разному → привести к одному названию.
- **[текст] Итог сессии 8** — «Теперь разделы 19 — письменно, как финальная проверка.» → рассогласование числа: «разделы» во множественном числе при одном номере «19» → «раздел 19» (если имеется в виду только он) либо явный диапазон «разделы 17–19».
- **[текст] Раздел 19, «Браузер и модули»** — «Чем ESM отличается от CommonJS помимo синтаксиса — статическая vs динамическая природа?» → в слове «помимо» последняя буква — латинская «o» вместо кириллической «о» (проверено побайтово) → исправить на «помимо».

### Часть 4 (шаги 2.2–4.2)
- **[противоречие] Шаг 2.2 и далее (сквозная проблема)** — «docker compose exec dev node src/playground/04_arrays.js» → все команды запуска во всей методичке построены вокруг `docker compose exec dev ...` / `docker compose --profile ... up -d` (сервисы dev/api/web), а в этом же файле уже записана находка: «У лабы в репозитории нет docker-compose.yml/Makefile... так и задумано (без Docker)» → нужно решить: либо в репозитории должен появиться docker-compose.yml с этими сервисами, либо все команды в методичке переписать на голый `node ...` / `npx json-server ...` без Docker.
- **[тех] Шаг 3.1 (05_this.js, блок «метод, оторванный от объекта»)** — «this внутри arrow всё ещё смотрит на cart (лексически)!» и «Последний эксперимент... сработал для printItems_arrow, потому что стрелочная функция помнит this лексически» → неверно: printItems_arrow — обычный метод (не стрелочная функция), при отвязанном вызове `loosePrint()` его собственный `this` — `undefined`, поэтому `this.items` бросает TypeError ещё до того, как выполнение доходит до внутренней стрелочной функции. Проверено (`node`): реально получаем `упало: TypeError`, а не `→ bolt`/`→ nut` из ОЖИДАЕМОГО РЕЗУЛЬТАТА → исправить: показать, что этот кейс тоже падает (аналогично printItems_bind), убрать неверное утверждение и неверные строки ожидаемого вывода.
- **[текст] Шаг 4.2 (ПРОВЕРЬ СЕБЯ)** — «...из UrgentTicket и попробуйte создать экземпляр» → латинские «te» внутри русского слова → исправить на «попробуйте».

### Часть 5 (шаги 4.2–7.1)
- **[тех] Шаг 5.3 (timing.test.js, тест throttle)** — «throttled('first'); // проходит сразу ... assert.deepEqual(calls, ['first', 'third']);» и итог «tests 2 / pass 2 / fail 0» → тест в реальности падает: при `mock.timers.enable({apis:['setTimeout','Date']})` фейковый `Date` стартует с 0, а `lastCall` тоже 0, поэтому `remaining = 100-(0-0) = 100 > 0` — «first» НЕ проходит сразу, а откладывается; «догоняющий» вызов при срабатывании использует `args`, захваченные вызовом, который поставил таймер (`'second'`), а не последним вызовом (`'third'`). Проверено запуском `node --test`: реально `calls = ['first']`, `assert.deepEqual(['first','third'])` бросает AssertionError → должно быть `fail 1`, а не `pass 2 fail 0`. Нужно переписать throttle (передавать актуальные args в отложенный вызов) и/или тест (не мокать Date либо иначе выставлять lastCall).
- **[текст] Шаг 5.3 (блок «Зачем»)** — «mock.timers.tick(ms) «промaтывает» время мгновенно» → латинская «a» внутри русского слова «проматывает» → исправить.
- **[тех] Шаг 6.2 (main.js, «Зачем» и «ПРОВЕРЬ СЕБЯ»)** — «Делегирование — один addEventListener('change', ...) на #ticket-list обрабатывает клики по select-ам» и «...убрать проверку ... и просто попытаться прочитать event.target.value у любого клика по списку (например, по span с заголовком)» → слушатель подписан на событие `change`, а не `click`; клик по `<span>` вообще не породит `change`, так что описанный сценарий не воспроизведётся как написано → заменить «клики» на «change»/«смену значения» по тексту.
- **[противоречие] Шаг 5.2/5.3/6.1 (продолжение находки из части 4)** — «docker compose --profile api up -d», «docker compose --profile web up -d», «docker compose exec dev sh -c "echo restart web"» → тот же сквозной Docker-паттерн, что в части 4. Отдельно команда `sh -c "echo restart web"` ничего не перезапускает (просто печатает строку внутри контейнера), а соседний комментарий тут же говорит «или просто обновите страницу» — сам шаг не несёт смысла и только запутывает.

> ⚠️ Уточнение к шагу 8.3 (мок модуля): предложенный `t.mock.module()` в Node 22 всё ещё экспериментальный — нужен флаг
> `node --test --experimental-test-module-mocks`. Альтернатива без флагов — передавать api в store как зависимость (DI) и подменять её в тесте.

### ✍️ Решение по вычитке
- [ ] ⭐ A — исправить всё из вычитки выше
- [ ] B — исправить только [тех] и [противоречие]
- [ ] C — выборочно (отметь пункты выше)
